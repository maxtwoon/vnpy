[pytest]
markers =
    realdb: requires local historical sqlite database
    slow: long-running tests
filterwarnings =
    ignore:Call to deprecated class TushareProApi:DeprecationWarning
addopts =
    --basetemp=.pytest_tmp

