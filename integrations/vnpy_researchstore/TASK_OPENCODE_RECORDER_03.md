# OpenCode development assignment 03 — recorder bridge and usable controls

Dispatch only after consumers02C has finished with .coordination/opencode-consumers02c-handoff.md and released CLI/config/tool ownership, and the actual recording-core owner has published exact journal/session APIs in INTERFACES.md plus .coordination/recording02-api-handoff.md. The queued recording-core assignment is TASK_OPENCODE_RECORDING_02C.md; its actual actor is OpenCode after explicit dispatch. References to Kimi APIs below mean that assigned core owner's public APIs. Codex coordinates, actual OpenCode develops, actual Claude Code audits/tests. Read IMPLEMENTATION_PLAN.md mandatory amendments, PLAN_AUDIT_DISPOSITION.md, INTERFACES.md and the latest developer handoffs. Personal local research: a small usable app, no additional service framework.

Own vnpy_researchstore/recorder.py, recording_engine.py, app.py, ui/**, recorder launcher/configuration, recorder bridge/UI tests, recording documentation. You may finish recorder CLI wiring in research_store/cli.py using Kimi's implemented APIs. Coordinate package entry points through the core owner. Do not edit Kimi's journal/aggregation/sealing/session recovery internals or vnpy core/site-packages. Send exact API gaps to .coordination/opencode-core-requests.md and continue independent work. No root HANDOFF changes.

Provide ResearchRecorderApp/Engine, RecordingMainEngine and an actual documented CLI/UI entry. Keep pure research_store free of vnpy/Qt imports. The launcher uses an isolated runtime directory and explicit repository core path before importing transitive vnpy utility modules; no changes to the existing .vntrader or default database. Recording writes only through the journal API, never the read-only snapshot Database bridge.

Only explicitly configured real instruments, exchange, source identity and gateway type are admitted. Preserve source/event kind and full copied payload. Reject LOCAL synthetic contracts. Separate simulated and production source IDs. A CTP TickData observation is quote/snapshot data unless the provider contract establishes another kind; do not call it exchange trade-by-trade. No automatic gateway/account connection or strategy loading. Existing user authorization does not authorize a real account connection for this assignment. Offline events must be visibly labelled test/simulated.

Callback acceptance copies event data and assigns/returns a sequence through Kimi's bounded journal admission API. Show state, source, instruments, accepted_seq, committed_seq, backlog, rejected count, last error, session_id and journal path. Error/overflow stops admission visibly and never silently reports success. State rendering must not itself block the shared event thread. Provide start, stop, retry-stop and status actions; do not inherit the old recorder's configurable 1–60 second memory-flush UI.

The stop protocol is binding: RecordingMainEngine.close enqueues RecorderStopBarrier BEFORE ANY parent close/EventEngine.stop. The barrier handler runs on the still-running dispatch thread after earlier queued tick events, unregisters recorder callbacks there, fixes accepted cutoff and starts/requests drain. The controlling thread waits at most10 seconds for journal committed==cutoff and CLOSED. Only then call super().close. If timeout/error, report STOP_FAILED, keep event engine/process usable for retry, and do not claim normal closure. Never block waiting for a barrier while on the dispatch thread; reject or marshal to a controlling thread. A UI closeEvent must honor a failed stop by ignoring the close rather than unconditionally exiting the window/process.

Wire separate import recover, recover-session, replay and seal commands with real typed receipts and nonzero failures. Recover only committed events; recovered old sessions become UNCLEAN_END and any new recording uses a new linked session ID. Seal/replay must not open a gateway. Final bars/late corrections/retention eligibility come from the core and remain visible; bridge does not fill gaps, change units, mark provisional output final or delete canonical files.

Development checks: actual EventEngine integration with queued ticks followed by stop barrier, callback mutation after enqueue does not alter saved payload, normal close exact cutoff, parent close is not called on timeout/write error, retry succeeds when drain becomes possible, dispatch-thread close cannot deadlock, repeated stop, disabled unconfigured instruments, source kind/SimNow isolation and no unsolicited gateway connect. Qt controls may be tested offscreen; document whether the real window was opened. Use a bounded child process for process termination/recovery where needed, never terminate unrelated Python/trading processes. Kimi owns low-level journal tests; avoid duplicating its whole suite unless bridge behavior creates new concerns.

Write .coordination/opencode-recorder03-handoff.md with changed files, exact commands/results, actual entry command, API gaps, remaining work and explicit LIVE_NOT_RUN. This assignment is not a declaration that whole v0.1 is complete.

Do not create or modify assistant/project memory. No dependency installs or shared environment/pyproject edits; request any packaging change from the explicit packaging owner through Codex. Actual journal behavior must be handed off before final bridge integration tests are called complete.
