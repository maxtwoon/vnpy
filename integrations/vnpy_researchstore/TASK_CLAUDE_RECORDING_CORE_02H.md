# Actual Claude recording-core02H — bounded independent audit

QUEUED. Dispatch only after actual Kimi aggregation02F final handoff, RECORDING_INTERFACES.md and affected source/dependency hashes stabilize. Actual Claude audits; Kimi/OpenCode develops, Codex coordinates. Personal local research, proportionate validation; no new enterprise requirements. Review the exact02F/dispatch contracts and final handoff. Reuse JOURNAL_RECHECK_02G PASS for unchanged foundation behavior; do not redo all journal/core/native audits.

Scope is new aggregation/sealing/retention and public recover/replay/seal wiring, plus02F's exact incremental models/catalog/revisions/journal modifications. Verify:

1. Committed-only selected sequence range, event-time/sequence deterministic replay, same-time events retained and session lineage. Missing time/calendar is not filled from received/natural date. First cumulative baseline, reset/gap/late/partial/zero-volume states are truthful, provisional bars do not become final qualified history. Include a session stopped mid-minute: CLOSED journal or final=True alone is not evidence that its last market interval completed; preserve partial/provisional meaning until actual interval/session evidence establishes completeness. Also test a last interval whose completion is actually evidenced, so the implementation cannot solve this by blindly losing every final interval.
2. Repeated seal with same session/range/transform/semantic/calendar/source config gives same output identity/content. Changes affecting output cannot hit old idempotency receipts. One meaningful interrupted publication leaves actionable partial/failure, never fully sealed; retry does not duplicate or lose events. Late correction is explicit new revision, old snapshot bytes/content preserved. Distinguish deterministic event-time replay from lateness at admission: ingest seq1/timeT0, seq2/timeT10, seq3/timeT1 must replay T0,T1,T10 yet preserve the fact that seq3 arrived late where the late/quality contract requires it. Sorting the replay must not itself be asserted as proof there was no late source event. Examine actual dataset/revision lineage: an idempotency range/session key must not arbitrarily masquerade as a changed semantic rule_version just to evade same-series conflicts. Repeated/range-extended/late-corrected output must follow the approved dataset identity and explicit revision model, not merely show different IDs.
3. Retention14days starts only after complete verified seal/publication. Active owner, partially sealed range, unverified/missing outputs refuse cleanup. Temporary tests prove resolved path boundary and symlink/junction escape handling; never delete real journal/raw/canonical/snapshot data.
4. Public recover/seal no longer PENDING. Typed last_error and successor ID match actual failure/session states; old/new identity and lock ownership are explicit, no prose parsing/private-field bypass or leaked writer. Verify documented source_spec/calendar_spec format/version/validation and asset/source semantics: supported recording inputs must not silently acquire a hard-coded wrong asset class; truly unsupported input should be explicit. Run synthetic/offline public seal->freeze->query with exact IDs/counts.

Use existing package .venv, focused meaningful regressions/type/lint, actual temp repro where assertions miss a concern. Rerun old journal repros only if02F modifies relevant behavior. No review of changing reader/snapshot dependencies; wait stable capture or explicitly record missing verification, never call SKIP PASS. Do not expand to real market authorization.

Only temporary artifacts in .coordination/review-recording02h. NO product/Git/assistant-memory changes anywhere, including stash/pop/drop/reset/restore/checkout/clean/commit/worktree; no raw/ledger/credentials/default .vntrader/vnpy core/site-packages/root HANDOFF/provider/network/account/gateway/model/quota writes/installs. Terminate only owned bounded test children. Return exact commands/hashes/results, per-item PASS/FAIL/PARTIAL, up to5 reproducible high-impact findings with file:line/trigger/minimumfix and FINAL_VERDICT scoped to recording core. Codex saves exact report RECORDING_CORE_AUDIT_02H.md.

Leave CLI JSON/exit codes, EventEngine stop barrier, UI failed-close/status, isolated bootstrap and wheel/sdist install tests to03D integration audit. Leave real same-snapshot Database/BOTHAlpha/CTA/Portfolio, dataset qualification, asset inventory/report/configs and global checks toWP10/final. Recording-core PASS alone is neither full v0.1 nor actual live capture. Preserve LIVE_NOT_RUN.
