# Actual OpenCode takeover — finish the real ETF04F loop

Explicit transfer from terminal Kimi04F session_a9a30707-7704-46a3-8c0f-b7df19a2a547 to a NEW actual OpenCode session. Execute current TASK_KIMI_DELIVERY_04F.md in full, preserving partial helper/tests/configs/evidence and existing store/snapshot. No stale old OpenCode assignment. Original partial failure was native vendor-ID resolution; actual OpenCode native06 now TERMINAL0 with .coordination/opencode-native06-handoff.md and current hashes/tests/read-only smoke. Its source ownership is released; this task may use it READ-ONLY, never change native code. Independent actual Claude combined qualified/native review remains pending, so this is developer validation, not final approval.

Use store-398306491d834f99 / snap-030369f20bd18303 unless a genuine public correctness need requires explicit new immutable snapshot; never reinitialize or overwrite it. Preserve exact original source hashes, all133repair-index loading, real import counts/idempotency and existing evidence. Reuse verified unchanged costly source work, finish same-snapshot Database+BOTHAlpha comparisons with normal510130.SSE/510300.SSE, inclusive/nonaligned/extended windows, original VWAP/OHLC math and zero-volume behavior, plus offline CTA/Portfolio bootstrap reading. No compound-symbol workaround or synthetic replacement. Build one reproducible runner with actual isolated runtime/module origins and exact counts/IDs/configs.

Only the original04F NEW helper/test/config/evidence names may be edited; existing modules/native/CLI/core/packaging and other owners' files read-only. Final handoff .coordination/opencode-delivery04f-handoff.md; original Kimi history unchanged. Root02I still writes unrelated recording-core and03D CLI/UI/packaging; record before/after dependencies and precise moving-code limitation, then final stable checks are rerun only if affected. Do not run whole package or their tests. OpenCode04K independently owns NEW delivery_representatives helper/data evidence,04H SS captures/helper; do not touch either. Full WP10 final docs/config names/HTML/audit remains separate after these scopes finish.

All original constraints apply: no source/raw/defaultsettings/ledger/credentials/corevnpy/sitepackages/rootHANDOFF/archived/unrelated-project/memory writes, provider/network/account/gateway access, purchases/environment upgrades or Git mutations ANY kind including stash/pop/reset/restore/commit. Existing plugin .venv; public API immutable store writes and named unique reports/configs only. Exact defects go to coordinator as reproductions, never compatibility tricks to force a pass.
