# Actual Kimi correction04 — enforce default repaired-data exclusion

CONTINUATION SAFETY CORRECTION: read .coordination/QUALIFIED04_INTERRUPTION.md first. The first attempt was terminated by Codex for executing git stash/pop while testing the active journal worker's new tests. All tracked content/index were reconciled; retain partial product changes. Do not execute ANY Git mutation (explicitly including stash/push/pop/drop/reset/restore/checkout/switch/clean/add/rm/commit/worktree/merge/rebase or reference writes), do not touch another worker's files, and do not run whole-package or journal suites. A plain stash cannot isolate this untracked integration. Journal failures during its active development are not a historical baseline or your finding.

Codex dispatches one bounded actual Kimi task after all product writers and consumer03 reviewer are terminal. Actual core/native Claude rechecks PASS; consumer03 has one accepted HIGH defect. OpenCode is currently quota-limited (reported reset2026-09-17 04:03:31 local); no OpenCode writer is active. Do not resume any old task. This new session owns only the minimum complete correction below, not recording or WP10.

Read CONSUMERS_AUDIT_03.md, CONSUMERS_AUDIT_03_DISPOSITION.md, .coordination/COORDINATOR_IMPORT_REQUIREMENTS.md, the actual reviewer reproduction .coordination/review-consumers03/repro_default_qualified_gap.py, current INTERFACES/NATIVE_INTERFACES and latest actual OpenCode handoffs. Personal local research: one practical local policy using existing storage/snapshot/quality mechanisms, no new service or governance architecture. Codex coordinates, actual Claude audits; do not edit coordinator task/plan/status/audit/disposition files.

Explicit temporary ownership: research_store quality/reader/snapshot/public policy interfaces and their necessary model/public-export changes, affected importer/CLI/export/report callers, native admission/read helpers and related tests/docs, ONLY where needed for this defect. No other developer is modifying those paths. Do not change journal/recording (pending), shared environment/pyproject, datasource mappings/units/ledger or unrelated code. Preserve all reviewed corefix03/nativefix03 behavior; identify the exact changed dependencies for focused recheck.

Binding behavior: ALL133 repaired ETF keys are excluded from DEFAULT qualified backtest data, including deterministic repairs equal to upstream candidates, not only118/119 disputed subsets. Current store_default_qualified counts exclusions but public freeze→reader/export/native paths still return them. Implement actual default enforcement and honest gaps/counts using existing public APIs. Do not close this by documentation-only wording, optional qualified-only mode with permissive defaults, hardcoded real test keys, whole-dataset rejection of unrelated clean windows, or silently treating a quality-report call as a required hidden precondition.

Retain raw observations and old/new/upstream candidates with provenance. Excluded rows must not reappear when the user explicitly allows the known gap: the allowance permits the missing interval, not the rejected row. No filler. Any explicit observational access must remain distinguishable and must not bypass default native qualified reads. Capture decisions consistently with immutable snapshot semantics; new policy decisions/revisions cannot silently rewrite an existing snapshot's meaning. Reuse applicable existing KnownGap/quality/error contracts and document any necessary additive public choice clearly.

Prove behavior through public store→freeze→reader and Database/BOTH Alpha methods, and CLI export/write/readback. The actual reviewer repro must now refuse or exclude the two flagged rows by default and produce consistent counts/gaps, while clean-range reads remain usable. Exercise deterministic matching repair, disputed repair, explicit gap allowance, old/new snapshot behavior, default callers that never ran the quality report, candidate preservation and ordinary clean datasets. Keep all actual source files readonly; fixture tests clearly labelled, real WP10 validation remains later. No fake qualification from a report or synthetic result.

Use delivered package .venv and read-only Studio Ruff; no installs. Run affected tests/type/lint and the original repro, not unrelated archived/full-history work. No assistant/project memory writes; no credentials/raw-source/default .vntrader/ledger/vnpy core/site-packages/root HANDOFF/unrelated-project/git changes, account/gateway/provider/network queries, quota purchases or provider/model changes. If quota refuses, stop with explicit failure; no self-retry loop or alternate developer.

Write .coordination/kimi-qualified-fix04-handoff.md with the chosen public default policy, exact APIs/files, actual repro and tests/commands/results, preserved snapshot semantics, limitations and remaining owner requests. Update product INTERFACES/NATIVE_INTERFACES only as needed. Do not claim whole v0.1 complete. Actual Claude will independently recheck this single defect and affected behavior.
