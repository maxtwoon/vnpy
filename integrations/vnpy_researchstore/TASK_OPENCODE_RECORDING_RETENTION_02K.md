# Actual OpenCode bounded recording02K correction

Continue SAME recording developer session ses_f5335ad96ffe2KeRp3gKXiQjNI. Codex coordinates; actual Claude audits. Read RECORDING_RECHECK_02J.md and RECORDING_RECHECK_02J_DISPOSITION.md first. Only the one independently reproduced exclusion/retention data-loss finding is open. Original F1-F4/calendar/null-time/zero are CLOSED; no new broad refactor.

Exclusive product writes: research_store/sealing.py, retention.py, and additive models.py receipt fields ONLY if necessary for truthful public status; directly relevant tests/test_sealing.py and test_retention.py; RECORDING_INTERFACES.md and .coordination/recording02-api-handoff.md; own .coordination/recording02k-dev and opencode-recording-retention02k-handoff.md. No CLI/UI/recorder/importer/native/README/VERIFICATION/packaging/core-policy/unrelated changes. Other actual developers remain active in those separate scopes. No Git/memory/raw/main D:/quant-data/defaultsettings/ledger/credentials/vnpycore/sitepackages/rootHANDOFF changes, network/account/gateway actions or environment installs.

Record actual before reproduction using unchanged .coordination/review-recording02j/repro_excluded_event_retention.py against temporary data. Do not edit or replace original reviewer evidence. Correct durable seal completeness/exclusion evidence and its public receipt/idempotent replay so successful known-time publication does not falsely certify lossless raw preservation. Retention must refuse deletion while even one committed raw event is only in its journal. Persist the required facts atomically enough for existing recovery guarantees. Legacy seal records lacking completeness evidence must not acquire a fabricated complete status; conservatively refuse or verify against available journal/publication evidence. No new force-loss flag and no fabricated ts=0. Preserve correct raw0/source day semantics.

Meaningful focused regressions: mixed known/unknown time cannot become cleanup eligible after15days and apply_retention keeps journal/event; repeated seal returns truthful durable exclusion/completeness; all-unknown-time input cannot falsely certify complete; legacy/missing evidence cannot authorize data loss; normal fully preserved aged seal can still clean up. Keep existing partial-range/path-bound guarantees. Do not compare tick+bar row totals blindly, as bar aggregation is a separate derived representation.

Run the unchanged actual Claude repro after fix, relevant sealing/retention tests and directly affected regressions, scoped Ruff/mypy. Reuse unchanged journal race/calendar/asset evidence unless touched dependencies justify rerun. Report exact commands/exits/results, changed paths and final hashes, limits and API changes EARLY if receipt shapes change. Finish with final handoff and exit; root will resume the SAME actual Claude02J session for only this finding and directly affected positive behavior. Personal research minimum implementation, no additional service or governance.
