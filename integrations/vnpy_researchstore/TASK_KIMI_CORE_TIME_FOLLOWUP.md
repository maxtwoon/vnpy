# Kimi bounded core follow-up — time contract and integration requests

Dispatch after core01b terminates; never run two Kimi writers on these paths. Read TIME_CONTRACT_REVIEW.md and TIME_CONTRACT_DISPOSITION.md, latest INTERFACES.md, and .coordination/opencode-core-requests.md if present. This is authorized refinement of the approved unknown-time semantics, before Claude's core implementation audit. Do not start recording02 yet.

Own existing core files/schema/tests/interfaces/pyproject only. Implement minute trading_date NULL preservation, daily NULL rejection at publication, daily identity=(instrument or series,trading_date), correct dedup/conflict/order/snapshot/query behavior. Keep normalized bar_start/bar_end mandatory for canonical bars. Missing day remains explicit quality metadata and no field derives it from a natural date. Core observation reader must round-trip such minute rows; freeze must retain uncertainty, not silently label qualified backtest data. Do not implement OpenCode's source adapters/profile/consumer code.

Address concrete OpenCode core insertion/API requests that fall in your ownership, documenting each disposition; do not simply invent a second publication path in the importer. Requests that expand scope or require uncertain semantics go back to Codex. Update INTERFACES.md to the actual tested contract and communicate any entrypoint/dependency hooks for subsequent CLI/adapters.

Use .coordination/CORE_REQUESTS_DISPOSITION.md for the importer requests. In particular declare/install zstandard only in the package .venv and rerun the actual importer/core integration test in that delivered environment; do not rely only on OpenCode's temporary test environment. Recheck final reader/import/snapshots syntax rather than treating in-flight observations as either permanent defects or automatically resolved.

OpenCode updated request4 with Windows objects._fsync_path O_RDONLY/fsync Bad file descriptor, reproduced across first-pass publication tests. Fix the durable-write implementation without swallowing sync errors; coordinate removal of its temporary strict-xfail after the fix and verify the real boundary. If core01 has already implemented these requested corrections, reuse exact-code valid results and document itemized evidence; do not repeat changes or full suites solely because this is a new task turn.

A separate OpenCode native02A worker now owns native adapters only; inspect .coordination/native-core-requests.md if it exists for additional exact API/dependency requests. It must not edit core or install into the shared venv. Keep core interfaces current for both OpenCode workers.

Current actors changed after OpenCode quota: native02B and consumers02B are disjoint actual Kimi workers. Read native-core-requests.md and consumer-core-requests.md if present; never edit their files. Core01 has now completed exit0 with117tests and9-owned-file mypy PASS. Its handoff describes daily requirement as trading_date AND instrument identity; reconcile precisely with the approved instrument-OR-series plus trading_date key. Daily continuous candidate series must not be excluded merely because instrument_id is null if series_id is valid. Explicitly prove duplicate/conflicting daily rows with different timestamp labels follow the trading-date key. Reuse unchanged valid checks; add only missing substantive evidence/fixes and write the named time handoff for Claude.

Run affected focused tests, Ruff and scoped typing as appropriate; meaningful cases include duplicate daily date with differing label timestamp, same key different value conflict, minute unknown day roundtrip, daily unknown day rejection, identity distinction for distinct minutes, quality retained in old snapshots. Preserve other ongoing developers' files. Write .coordination/kimi-core-time-handoff.md with exact implemented APIs/tests and remaining requests. Actual Claude will then review the complete stable core milestone.
