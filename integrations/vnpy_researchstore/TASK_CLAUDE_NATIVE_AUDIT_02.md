# Actual Claude Code bounded native implementation audit

Codex coordinates; actual Kimi/OpenCode develop; you are actual Claude Code independently reviewing the completed native02B milestone. Read IMPLEMENTATION_PLAN mandatory amendments, PLAN_AUDIT_DISPOSITION, TIME_CONTRACT_DISPOSITION, INTERFACES, NATIVE_INTERFACES and .coordination/kimi-native02b-handoff.md. Native02B has terminated exit0. Core-time also terminated; a separate actual Claude core audit is running read-only. The consumer/importer/CLI worker still owns changing files; do not audit that moving implementation or run the whole integration suite.

Personal local research, minimum practical verification. No product edits, credentials/accounts/gateway connections/network source queries, git lifecycle, raw/store/default .vntrader/ledger/core/site-packages writes. Temporary fixtures and reproductions only under .coordination/review-native02 or system temp. Do not scan CLI JSONL transcripts as authored project instructions. Use D:/repo/vnpy/integrations/vnpy_researchstore/.venv/Scripts/python.exe; record actual executable, module origins and hashes of reviewed native files so final review can reuse unchanged evidence. Codex saves your exact final report and sends fixes to the actual developer.

Bounded owned scope: vnpy_researchstore/__init__.py, database.py, alpha.py, bootstrap.py, native_common.py; tests/test_native_*; tools/native_*; configs/native_*; NATIVE_INTERFACES. Core may be read as an API dependency, but its separate review covers publication/recovery/streaming and schema internals. Any verified core-dependent defect should be labelled core owner, without implementing a bridge bypass.

Verify real native caller contracts, not just test counts:

1. Database snapshot-only resolution, no live heads/globs, source ambiguity and unmapped exchange explicit; saves/deletes/native ticks refuse, unsupported5/15m overview omission with diagnostics, no relabel. Default missing/untrusted auxiliary refusal and explicit OHLCV-only NaN/metadata mode. Equities OI=not-applicable differs from unknown futures OI. Candidate/unknown semantic flags must not become qualified bars simply because values are non-null.
2. Exact inclusive native endpoints for Database and BOTH Alpha methods, equality and nonaligned times, daily trading-date identity versus stored bounds, timezone-aware and naive caller conventions against actual BaseDatabase/Alpha/CTA/Portfolio code. Unknown minute trading_date must retain uncertainty and be refused only where required by contract. No natural-date guessing or silently dropped requested row.
3. Alpha native signature/window/extended_days/empty-result contract, original OHLC first-close normalization and suspension behavior, VWAP units, zero volume, NULL/untrusted turnover. Futures multiplier must have verified effective applicability and single-side units, not merely a plausible number. Verify actual code's meaning of allow_missing_auxiliary for each API; a feature path cannot manufacture VWAP from missing/untrusted fields. Snapshot-bound results and write refusal.
4. Bootstrap fresh-process ordering: explicit repo/module origin and isolated cwd before transitive utility imports, DB_TZ import-time configuration, existing singleton refusal, actual plugin class/snapshot assertion BEFORE CTA/Portfolio import, no SQLite fallback or default .vntrader modifications. Use shipped entry commands in bounded subprocesses. Package import must stay lazy. Configuration and launcher instructions must match actual APIs.

Run the39 native tests and affected scoped lint/typing once, and only focused temporary repros for concerns missing from those assertions. Use existing Studio Ruff executable read-only if .venv has no Ruff; no install needed. Record command/result. Full real-data two-consumer delivery is assigned WP10 and not available yet: mark NOT_RUN, do not misclassify it as a native code bug or approve whole v0.1. Native wheel inclusion is a known core-owned final packaging task, also not a new native bug.

Return at most4 highest-impact verified concrete findings with priority/path:line/trigger/evidence/minimumfix/owner, checks actually proven versus unverified, and PASS/FAIL for this native milestone only. Do not block on style preferences or introduce an enterprise checklist. Findings remain open until actual developers fix them; no direct edits.
