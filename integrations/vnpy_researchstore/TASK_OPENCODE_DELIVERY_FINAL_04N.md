# Queued actual OpenCode final practical delivery WP10

This is the final assembly of TASK_OPENCODE_DELIVERY_04, not a new scope or permission to call incomplete requirements passed. Actual OpenCode develops, actual Claude independently audits, Codex coordinates. Read the full approved task and current implementation plan. Preserve personal research simplicity: one local plugin, runnable configs, clear lightweight standalone HTML, truthful status, no backend/service.

Exclusive writes after coordinator dispatch: README.md, VERIFICATION.md, CHANGELOG.md; NEW tools/delivery_finalize.py plus meaningful focused tests; final smoke_import/smoke_snapshot/recorder_replay/backtest_profile repository templates and real D:/quant-data/configs instance configs; reports/delivery04n, .coordination/delivery04n-dev and opencode-wp10-handoff.md. Existing source/modules/other helpers/configs, TASKS/plan/audit/task files are read-only. Actual03D separately owns package/CLI/UI and INTERFACES/RECORDER_INTERFACES. Avoid edits to its paths; use stable public commands. Do not silently fix another owner's defect or manufacture an acceptance wrapper; return a precise reproducible missing public behavior to root.

Two phases are permitted. Before final source release, assemble existing measured inventory/capture/04F/04K/04L/04M/03D facts with explicit PENDING dependencies; no final PASS/effective instance config referencing nonexistent output. After the coordinator releases corrected source and required reviews, run the final affected checks and bind outputs to final hashes. Reuse unchanged valid evidence; rerun only when a changed dependency affects its claim.

Required actual evidence: SAME real ETF snapshot through core, Database, BOTH Alpha methods and offline CTA/Portfolio bootstrap; historical raw-stock target=store; scoped SS capture/time evidence/real contract/1-5-15m/SimNow8/MA; real RQ futures source candidates with UNKNOWN label and exact sourced night trading_date; JQ33cols/NULL/unknownadjustment; all133repaired-key default exclusion; P4 missing/version/PIT facts; real offline synthetic-labelled journal admission/clean stop/unclean recovery/replay/seal/freeze/query/retention boundaries plus packaged CLI/UI launch and B1 failure behavior. LIVE_NOT_RUN stays visible. Inventory discovery, software capability and qualified dataset/range are separate dimensions.

Deliver real runnable configs using actual existing paths, store ID, dataset/revision/snapshot/session IDs from verified runs. Keep repository templates visibly generic. Include exact one-step commands and isolated runtime dirs; no .vntrader defaults changed. Provide raw-source expansion commands without requiring full54GB ingestion. Preserve current-failure/unknown/source limitation details without turning them into unsupported software success.

Standalone report should show source inventory and capture validation, currently usable ETF/stock ranges, unknown/repair/quarantine gaps, observed/accepted/expected counts (expected UNKNOWN where unsupported), source conflicts and versions, recording watermarks/errors/offline-vs-live, disk usage and exact verification status. No bulk purchased data embedded. Reuse public report API where available rather than duplicate business logic.

After all relevant source is stable: run focused new integration tests and FULL existing vnpy_datasource suite, Ruff both integrations, scoped mypy, package build/noneditable entrypoint checks (reuse current03D wheel/sdist evidence if unchanged), pure-core import without vnpy/provider side effects, root tools/sync_check.py. Compare datasource24-file baseline and preserve allowed additive changes/legacy behavior, original root HANDOFF and unrelated baseline. No environment upgrade, no Git mutation or weakening checks. Store precise commands/exit/counts/hashes/limitations and minimal machine-readable verification matrix keyed by WP/check_id/dimension.

Final opencode-wp10-handoff.md must bind first usable snapshot, ready scopes, current candidate/unknown inputs, exact file changes, report/config paths, one-step usage and rollback (original launcher/environment), all checks and remaining failures. Developer completion is pending actual Claude final audit, not final independent approval. No placeholders presented as IDs, no silent omitted mandatory feature. No Git/memory/raw/ledger/credentials/defaultsettings/vnpycore/sitepackages/rootHANDOFF/archived/unrelated-project writes, network/providers/accounts/gateways/purchases or shared-env installs.
