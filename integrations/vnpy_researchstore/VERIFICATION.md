# VERIFICATION — v0.1.0.dev1 (delivery04N phase 1)

Scope: this file assembles EXISTING measured evidence with explicit pending
dependencies. It is a development-phase summary, not an independent audit.
The machine-readable matrix (same content, bound to evidence-file hashes,
regenerated by `tools/delivery_finalize.py report`) lives at
`reports/delivery04n/verification-matrix.json` with a standalone HTML render.
Rule: facts below are extracted from machine evidence; prose claims from
developer handoffs are labeled as claims. Store-global `verify` counts are
work units of that verify run, NOT per-snapshot object counts.

Status legend: MEASURED (machine evidence bound) · PARTIAL (measured but
scope incomplete) · PENDING (owned by an in-flight task) · NOT_RUN ·
UNKNOWN (cannot be known in scope) · LIVE_NOT_RUN · EVIDENCE_MISSING
(bound evidence disappeared — matrix only).

## 1. First usable snapshot — real ETF same-snapshot loop (MEASURED)

* Binding: store `store-398306491d834f99`, snapshot `snap-030369f20bd18303`,
  datasets `ds-d939147fd6b633fd348fa62ed1fef74c` (1d, 488 rows, partition
  `2016`) and `ds-b9bbad83ec09f4b9dcef6ea9a81c4b51` (1m, 117,120 rows
  imported across 25 January partitions; snapshot pins `XSHG/2016-01/`
  bucket-03 + bucket-05, 4,800 rows each).
* 16/16 checks PASS, exit 0 (`tools/delivery_etf_loop.py`, runner sha256
  `3fd949a6c870d8336647420e06b6abc998c7e914df407be35a88971e0ce4ef6d`,
  evidence `.coordination/delivery04f-dev/opencode-loop.rerun.stdout.json`):
  core SnapshotReader 1,920 minute / 8 daily rows; Database and both Alpha
  methods digest-identical on bare native symbols; inclusive end (14:59
  included), nonaligned end (14:58:30 → 959 rows, last 14:58), extended
  windows (2,640 minute / 11 daily rows per symbol, Jan 8–22); lunch edges
  (240 bars/day, no 11:30–12:59 bars); source END-label → native START on
  every row; pinned source rows reproduced exactly; zero-volume minutes keep
  OHLC 3.25 with missing (NaN) VWAP and the 13:01 bar VWAP = 3.275; overview
  presents native symbols only (4,800 minute / 244 daily rows per symbol);
  offline CTA (960/4 rows per symbol) + Portfolio (1,920/8 rows) bootstrap
  from a fresh child bound to the SAME snapshot; read-only verify ok;
  import idempotency + 133-key repair-index receipts re-proven.
* The first run of this loop scored 13/16; independent Claude review
  reproduced all three failures as runner assertion defects (not product
  defects) — dispositions in QUALIFIED_NATIVE_RECHECK_06.md; fixes applied
  to the owned runner only; the FAIL output is preserved at
  `.coordination/delivery04f-dev/opencode-loop.preserved-FAIL.stdout.json`.
* Standing limits: `expected_coverage` UNKNOWN (no calendar/listing
  evidence); qualified range = 510130/510300 SSE, 2016 frozen selections
  only.

## 2. Inventory and representative validations (MEASURED)

| Item | Measured facts (source file) |
|---|---|
| Both source roots | SS 123 files / 19,667,963,677 bytes; holographic 3,608 files / 53,907,819,144 bytes; 1,426 tar.zst archives listed; no bulk hashing (`D:/quant-data/reports/delivery04d/delivery04d-evidence.json`, re-checked by Claude 04E) |
| Inventory fix 04B | failed/unrecognized archive-list records preserved, path escapes refused (`delivery04b-evidence.json`) |
| Stock raw target=store | delivery04k `stock.json` (real Client result, records/NULL/metadata preserved) |
| JQ 33 columns / NULL / unknown adjustment | delivery04k `jq.json` (query capability ≠ return-backtest qualification) |
| ETF 133 repaired keys default exclusion | delivery04k `etf.json` (disputed observations excluded by default, visible) |
| P4 catalog-only + missing overlay + version difference + shares fallback + PIT limits | delivery04k `catalog.json` (catalog coverage is NOT import qualification) |
| RQ futures A2505 label conflict | delivery04k `futures.json` binds the measured conflict; resolution owned by 04L |
| Preserved failure record | `delivery04a-inventory-FAILURE.json` kept visible (fixed by 04B) |

## 3. Capture and recording (PARTIAL / PENDING / LIVE_NOT_RUN)

* SSQuant capture stage receipt `validated` (10,225,500,736 bytes both sides;
  `delivery04h-verify-capture-stage.json`) — PARTIAL: strict SQLite-header
  allowance + unchanged-input recovery receipt still owned by capture04H.
  Old worker completion stays UNKNOWN.
* SS representative import (1/5/15m isolation, SimNow8 key, MA untrusted
  amount → missing turnover, VWAP refusal): PENDING (04M). No receipt exists;
  nothing claimed.
* RQ futures night trading_date: 04IB-verified 1,035 exact mappings are a
  developer claim; public normalizer/spec END→UNKNOWN fix PENDING (04L);
  until then the direction label stays UNKNOWN.
* Recording engineering (journal admission, clean stop vs UNCLEAN_END,
  recover-session, committed-only replay, idempotent seal): covered by
  scoped test suites (unit level); durable real-store instance PENDING —
  `D:/quant-data/journals` is empty, so no instance replay/seal config is
  shipped (no placeholder IDs). recorder03D B1 launcher fix + installed
  entrypoint acceptance PENDING (03D). Calendar/NULL semantics fixes PENDING
  (02IA).
* `live_gateway_recording` = **LIVE_NOT_RUN** (no authorized live capture;
  label must stay visible).

## 4. Native bridge and reviews (MEASURED, developer-level)

* Bare native symbol resolution (`510130`/`510300` + SSE) verified on the
  real snapshot by the loop checks (section 1) and by the native06 read-only
  smokes; independent Claude qualified05/native06 recheck: FINAL_VERDICT
  PASS, 152 scoped tests, 53/53 input hashes unchanged
  (QUALIFIED_NATIVE_RECHECK_06.md — scope-limited acceptance).
* Unknown vendor suffix (e.g. `XZZZ`) → ordinary empty result (never an
  error, never guessed); unmapped exchange label on a bare identity →
  explicit StoreError; both pinned by tests. Database reports
  present-but-unmappable rows via `diagnostics`; ResearchAlphaLab has no
  overview-equivalent surface — disclosed asymmetry, tested, not a defect.

## 5. Final stable checks (NOT_RUN — phase 2, deliberate)

Full existing vnpy_datasource suite, Ruff both integrations, scoped mypy,
package build + non-editable entrypoint checks, pure-core import without
vnpy/provider side effects, root `tools/sync_check.py`, and the
datasource 24-file baseline comparison are **deliberately not run in phase
1**: they require stable completed dependencies (04H/04L/02IA/03D/04M).
Running them against moving code would produce results that could not be
bound to final hashes. Reuse rule for phase 2: unchanged valid evidence is
reused; only claims affected by a changed dependency are rerun.

## 6. What is intentionally absent

* No recorder replay/seal instance config (no real session ID exists).
* No final instance IDs beyond the verified ETF snapshot scope.
* No fabricated publisher attribution: publisher capability observed in
  prior integrations is not re-asserted here without a measured receipt in
  this store's scope.
* No bulk purchased data embedded in any report or doc.
