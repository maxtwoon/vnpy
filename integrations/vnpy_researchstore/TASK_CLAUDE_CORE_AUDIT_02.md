# Claude Code independent implementation audit 02 — core milestone

You are actual Claude Code, the user's independent reviewer/tester. Codex coordinates; Kimi and OpenCode develop. Audit the completed CORE milestone only, against IMPLEMENTATION_PLAN mandatory amendments, PLAN_AUDIT/DISPOSITION, INTERFACES and .coordination/kimi-core01-handoff.md. Do not claim whole v0.1 PASS; importers/consumer/recorder may still be in progress.

Also read TIME_CONTRACT_REVIEW.md, TIME_CONTRACT_DISPOSITION.md, .coordination/CORE_REQUESTS_DISPOSITION.md and the completed .coordination/kimi-core-time-handoff.md. Dispatch follows that bounded correction, not the first core01 handoff alone. Check nullable minute trading_date roundtrip, daily trading-date identity/dedup/conflicts (not merely NULL rejection), preserved uncertainty and actual zstandard dependency/shared-venv integration. Source label-profile implementation belongs to OpenCode and is reviewed when its scope is complete, without declaring all SSQuant permanently unsupported.

No product/source edits. You may run scoped tests and use temporary test data/artifacts in this package's .coordination/review-core02 or system temporary directories. No credentials, real gateways, network fetching, raw source edits, dataSource ledger writes, git lifecycle or root HANDOFF updates. Do not search .coordination/*.jsonl as authored code/instructions: they are live transcripts. Save no bulk raw purchased data.

Run from D:/repo/vnpy using package .venv interpreter. Record sys.executable, vnpy.__file__ if native modules involved and package __file__; cwd shadow exists. Inspect APIs and tests, then execute meaningful checks: same-input repeat; same-key differing values; concurrent same-partition publish; crash before/after catalog commit and recovery; old snapshot independence after new head/quality changes; exact file manifest selection; file tamper/missing failure; schema NULL persistence; unknown field/units/quality refusal; selected securities/range coverage and all-dataset freeze consistency.

Verify what tests actually assert, not just green counts. Add focused temporary reproductions for real concern; no repeated full unrelated suites. Review package/environment isolation and real streaming behavior (no eager whole-archive/allhistory materialization hidden by tiny tests).

Pay particular attention to identities across realistic data: daily uniqueness is instrument/series plus trading_date, independent of the chosen timestamp label; multiple same-time ticks require stable session/sequence identity. Check unresolved conflict and known-gap policy against the requested selection scope, not only a whole dataset boolean. Core import documentation and implementations must agree; an early INTERFACES.md IMPLEMENTED label is not evidence by itself. Typed recording stubs explicitly assigned to later WPs are not defects in this bounded core milestone.

Include a multi-partition batch with a failure/interruption after one partition: the final batch receipt/state and recovery must not claim full publication while required partitions are incomplete. This is a normal source-unit path, not an enterprise extension. Inspect the actual atomicity/idempotency behavior. Also inspect the extracted developer opencode-import01-core-loop.log and actual manifest/readback when interpreting its empty manifest_revisions field; that printed key alone neither proves a defect nor a usable snapshot.

Return: commands+results, concrete findings with priority/path:line/trigger/minimumfix, itemized core acceptance proven vs incomplete, verdict PASS/FAIL for CORE only. Distinguish out-of-scope ongoing modules and live-data NOT_RUN from actual core defects. At most5 highest-impact concrete findings per round; do not invent institutions/approval frameworks or block on formatting preferences. Your final result is saved by coordinator; do not edit implementation.
