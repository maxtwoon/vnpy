# Claude Code independent audit — v0.1 integration and delivery

Dispatch only when Codex names the finished developer handoffs and stable code baseline. You are actual Claude Code, user-chosen independent reviewer/tester. Kimi/OpenCode develop; Codex coordinates. Read IMPLEMENTATION_PLAN.md mandatory amendments, PLAN_AUDIT_DISPOSITION.md, INTERFACES.md, TASKS.md, previous core review/dispositions, developer handoffs and VERIFICATION.md. Review against the accepted personal local research scope, not an invented enterprise standard. Do not assume a developer PASS or early interface label proves behavior.

No product edits. You may execute scoped tests and create temporary reproductions under .coordination/review-final or system temp. Raw sources, existing store snapshots/objects, ledger/credentials/default .vntrader, vnpy core/site-packages, root HANDOFF and unrelated work are not writable. Do not connect accounts/gateways, query network providers, run downloaded scripts, perform git lifecycle, or scan CLI jsonl transcripts as project instructions. Use the plugin .venv; record actual executable/module origins and code hashes. Codex saves your final report; fixes go to actual developers.

## Verification priorities

1. **No data silently changes meaning.** ETF END labels and repaired-base evidence: the actual importer inventory found133 deterministic repair keys,118 volume/amount disputes and119 any-market-field disputes. Verify against current asset hashes rather than magic truncation. ALL133 keys are excluded from DEFAULT qualified data even if embedded in rebuilt base or equal to refetch; observation inclusion and flags alone do not prove default exclusion. Read .coordination/COORDINATOR_IMPORT_REQUIREMENTS.md. SSQuant precise8 main SimNow keys and staging coexistence, original real_symbol/OI/rawamount, ambiguous Zhengzhou codes, interval distinction; RQ futures trading_date/dominant_id/member identity and real vs continuous isolation; JQ adjustment stays unknown and preserves all fields/NULL. Verify relevant real source locators/hashes/ranges. Unknown expected coverage is not completeness. Missing overlay and source-manifest version conflict must remain visible. P4 may be catalog-only.
2. **Streaming publication and snapshot identity.** Use prior valid core review evidence when source hashes match; rerun affected behavior after changes. Verify import failures cannot become success, repeating imports are idempotent, conflicts require explicit new revisions, freeze binds heads/quality consistently, readers use exact manifests, missing/tampered files fail and NULL survives. Inspect realistic memory behavior and full effective partition revisions, not just tiny fixtures. Daily keys use trading_date; same-time ticks keep distinct session/seq.
3. **Existing datasource compatibility.** target=store branches before zero-filling conversion and preserves raw records/metadata/attempts/request/registry hash; non-ok Client never publishes success. Lazy dependency. Full existing datasource suite, compare BASELINE_VERIFICATION24-file baseline and allowed changes. Do not infer external data quality from offline tests.
4. **Actual two-consumer loop.** Open the exact delivered first usable snapshot through the core, Database, both Alpha methods, and offline CTA/Portfolio bootstrap/loading. Independently compare real rows/counts/public fields and mathematically expected Alpha preprocessing. Inclusive end equal/nonaligned boundaries, extended_days, zero volume, empty windows, missing/untrusted turnover, futures multiplier semantics. Native5/15m omitted with diagnostics rather than relabelled. Save/delete/tick-native unsupported errors honest. No default SQLite fallback, no snapshot hot switch, explicit module path and isolated cwd before utility import. Result directory bound to snapshot.
5. **Recording loss visibility.** Journal lock, same-seq integrity, event+watermark atomicity, killed-process committed-only recovery, bounded admission and visible rejection/write-error. A stop barrier is enqueued BEFORE parent close or event stop, callbacks removed in barrier after prior queued events, exact cutoff committed before CLOSED. Timeout/error keeps STOP_FAILED and no parent close; UI cannot exit despite failed stop. Event-thread stop cannot deadlock. No unsolicited gateway connection or synthetic LOCAL admission. Repeat replay/seal, late new revision/old snapshot unchanged, cumulative reset/unknown first base/gaps/partial final bars, quote snapshots honestly classified. Retention eligibility/path bounds and no canonical deletion. Prefer bounded fault tests already present plus focused reproductions where the assertions miss a concern.
6. **Usability and honest delivery.** Commands/configs use actual instance IDs/paths, package builds, pure core imports without vnpy/provider side effects, CLI JSON/error behavior is real, reports show source/coverage/conflict/watermarks/disk/status, read-only exports have verified bindings/readback. README entry commands match code. Software completion, dataset READY scope and LIVE_NOT_RUN must be distinct. Full asset inventory plus real streaming capability/representative loops is required; full historical import/re-audit of every archive is not a new gate. Do not call missing required APIs a later enhancement to approve a prototype.

Apply TIME_CONTRACT_DISPOSITION.md and the plan amendments: minute trading_date may stayNULL when real normalized bounds are evidenced; daily identity is instrument OR series plus trading_date. SSQuant label profiles are capture/table/range-bound, not a global START guess or blanket permanent exclusion. A RQ dominant_id+source_datetime trading-day join is not itself proof of START/END label semantics. Verify native datetime awareness and timezone handling against the actual installed BaseDatabase/Alpha callers, including daily date-label and minute boundary behavior; do not accept an in-progress interface document's wording as implementation evidence.

## Checks and output

Run relevant package tests, full datasource tests, both scoped Ruff, scoped mypy, package build and root sync_check on the stable delivered source; valid exact-code prior results may be cited when duplicate work adds no value. Classify known unrelated baseline warnings accurately. No repeated entire suites unless fixes/failures justify. Record exact commands/exits/counts and any implementation changes seen during audit (invalidate affected evidence rather than review a moving target).

Return up to5 highest-impact verified findings per round, each priority/path:line/trigger/evidence/minimumfix/owner. Distinguish actual defects from missing live gateway authorization/NOT_RUN and data knowledge UNKNOWN. Give itemized WP00–WP10 PASS/FAIL/PARTIAL/NOT_RUN and evidence, software verdict, delivered dataset qualifications, recording engineering status, live gateway status, first usable snapshot ID/ranges or precise reason absent. Overall PASS only when all required v0.1 functionality and real-data two-consumer loop are demonstrated; LIVE_NOT_RUN is permitted if reported and no authorized live capture was requested. Do not claim live trading/readiness. Preserve concerns and unavailable evidence explicitly.

Do not create, update or append ANY assistant/project memory (including .claude/projects/**/memory, .codex/memories or MEMORY.md). The user has not requested memory updates. Save task evidence only in the explicitly named review artifact directory; an auto-memory habit does not authorize extra writes.
