# Actual OpenCode WP10K — remaining real representative evidence

Current source clarification08:49: actual FUTURES_TIME_REVIEW_04IB.md and disposition supersede04I. Labeldirection remains UNKNOWN; exact1035datejoin/hashes verified. Read .coordination/delivery04k-coordinator-inbox.md before futuresqualification. No guessedbounds or silentdefaultEND promotion.

Explicit bounded assignment, not the old whole-consumer task. Codex coordinates, actual OpenCode develops/runs representative validation, actual Claude audits. Personal local research: reuse current public APIs and one small reusable runner, no new services or full-history re-audit. Read full TASK_OPENCODE_DELIVERY_04.md, IMPLEMENTATION_PLAN mandatory amendments, source inventory04E/04D, current actual handoffs and source locators. Preserve unknown/failed qualification rather than manufacturing successful normalized data.

Exclusive writes: NEW tools/delivery_representatives.py, NEW tests/test_delivery_representatives.py only if meaningful, NEW configs/delivery_representatives_*.json, .coordination/delivery04k-dev, .coordination/opencode-delivery04k-handoff.md, public-API-created immutable store outputs and reports/delivery04k plus unique instance configs under D:/quant-data. Existing store store-398306491d834f99 must never be reinitialized. Other source files/modules, all existing helpers/configs/main docs/tests and all raw data read-only. Required minimal product defects must be returned with exact repro to coordinator, no consumer workaround.

Other owners: OpenCode02I owns recording core models/aggregation/sealing/journal/core exports; native06 owns native bridge;03D owns CLI/UI/packaging;04H owns SS capture helper/sqlite_source. Do not touch or test those moving features. ETF04F's helper/configs/snapshot and SS capture candidate are read-only. Use pure public core/importer APIs, no moving native/recording/CLI entrypoints. Final delivered acceptance requires stable dependency hashes; record before/after hashes and rerun only affected checks after owners release. Do not claim first-usable ETF native snapshot or full WP10.

Complete bounded actual source demonstrations:

1. Historical real Client raw-stock target=store: locate existing opencode-consumers02c-realstore-evidence.json and REAL_INPUT_CANDIDATES.md. Reuse verifiable identical-code evidence when valid; otherwise rerun through actual vnpy_datasource.storage.save_history(target=store) with the existing successful raw JSON receipts, preserving raw fields/NULL/metadata/request/attempts/registry hash. No new provider query or zero-filled BarData conversion. Record the actual instance config/output IDs and public readback/repeated-import counts.
2. RQ real futures A2505 selected night scope from FUTURES_INPUT_CANDIDATES.md. Actual Claude04I independently reviews local time-label/date/unit evidence; consume FUTURES_TIME_REVIEW_04I.md and disposition if available, otherwise independently inspect bounded source evidence and preserve conflict until resolved. Exact keyed real-contract/source-date join with Friday-night example, duplicate/unmatched/conflict counts, contract versus dominant-series isolation and uncertain roll rule. Never copy continuous prices or infer natural-date trading day. Only evidenced source bounds may normalize; UNKNOWN semantics stay queryable/inspectable source candidates with a concrete blocker. Validate existing streaming importer capability and honestly scoped resulting qualification. Never quietly pick START/END to make test pass.
3. JQ actual33-column sample from a real gzip member: preserve every source field/NULL and factor1=>adjustment UNKNOWN, demonstrate bounded public import/query where supported, no backtest qualification from query success. Select a small actual security/year/window and record all input/output counts and evidence; no whole-history materialization.
4. Actual ETF repaired-base sample using the full133-key correction index loaded before base, explicit old/new/upstream candidates and default qualified exclusion. Include a disputed real key, e.g.160105.XSHE2017-09-19 13:01 if current source hashes support it. Existing selected security/year streaming CLI is not required; use current public import APIs and bounded filtered streaming, do not implement a second importer or change another owner. Record repair counts, concrete source/member/row IDs, quality/gaps and snapshot behavior without globally allowing gaps.
5. P4 catalog-only truth: use corrected measured inventory04D, preserve missing convertible4911 overlay/audit, index2014 manifest/sidecar version conflict,37missing preferred shares years with older-source fallback, and PIT knowledge-time ambiguity. These categories remain catalog-only/candidate unless current independent evidence qualifies a bounded result. Do not rehash all54GB or turn catalog discovery into tested import capability.

Save machine-readable exact commands/exits, source/member/config/code hashes, actual dataset/revision/snapshot IDs, counts and comparison assertions, observed/expected coverage (UNKNOWN where calendar/listing/session evidence missing), elapsed/peak memory for representative streams when available. No placeholders presented as real IDs. New instance config names delivery_representatives_*; main smoke_import/backtest_profile final names belong final delivery owner later. Finalhandoff lists reusable runner commands, software failures separately from source/data limitations, and remaining final docs/HTML/fullchecks/ETF/native/SS/recording dependencies.

No Git mutation ANY kind including stash/pop/reset, assistant/project memory updates, raw/ledger/credentials/defaultsettings/vnpycore/sitepackages/rootHANDOFF/archived/unrelated-project writes; no network/providers/accounts/gateways/model/quota purchases or environment installs. Existing plugin .venv only. Temporary evidence under owned paths. Source scripts/pickle never executed. If task hits actual missing public behavior, save precise reproducible input/scope/error then continue independent representative checks; do not replace the required public end state with a private implementation.

