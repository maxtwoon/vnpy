# Actual Claude Code consumer completion review03

Codex dispatches against stable consumer code after actual OpenCode consumers02C has terminated. Its handoff was written before a provider429 quota exit1; do not turn the nonzero process exit into software acceptance or discard its actual test evidence. Read .coordination/opencode-consumers02c-handoff.md, consumer-core-requests.md, COORDINATOR_IMPORT_REQUIREMENTS.md, REAL_INPUT_CANDIDATES.md, TASK_OPENCODE_CONSUMERS_02C.md, IMPLEMENTATION_PLAN and current public interfaces. Actual Kimi/OpenCode develops, actual Claude audits, Codex coordinates. Personal local research, minimum useful scope; no enterprise release demands.

No product edits, dependencies, account/gateway/provider/network queries, source/ledger/credentials/default .vntrader/vnpy core/site-packages/root HANDOFF/git changes. No assistant/project memory writes anywhere. Temporary reproductions and evidence only .coordination/review-consumers03. Use plugin .venv and read-only Studio Ruff. Do not scan whole CLI transcripts. All product writers are currently terminal; another actual Claude core reviewer is read-only. Native correction03 has independently passed its bounded review, not full consumer qualification.

Focus on up to THREE concrete high-impact defects or required unimplemented consumer behaviors. This is not final WP10 acceptance, and not a request for full historical import. Prioritize:

1. The binding rule excludes ALL133 repaired ETF keys from DEFAULT qualified backtest data, not just118/119 disputed subsets. Consumer handoff now claims a store_default_qualified/quality-report policy while consumer-core-requests explicitly says snapshots do not drop rows and native admission is still needed. Determine through a minimal public store→freeze→Database/BOTH Alpha/query path whether those rows are actually refused/excluded by default or only reported as excluded. Include a deterministic repair matching the upstream candidate and a disputed repair, preserved candidate provenance and visible gaps/counts. A reporting count or a flag is not enforcement. Separate ordinary observational access from a qualified/native backtest path and state the exact failing command/API if behavior is missing. Do not broaden this into a new whole-system qualification architecture.
2. Direct datasource target=store preserves real pre-BarData records/NULL/units/historical metadata/request/attempts/registry hash and non-ok refusal; legacy sqlite/alpha compatibility. Read the actual 3x10 real input evidence and rerun a bounded representative check using existing readonly Client JSON if useful; synthetic NULL checks remain labelled. No network fetch is necessary or authorized.
3. Relevant CLI/export integrity: single JSON and meaningful nonzero failures for failed recovery/import/pending recording, receipt gap_ns round trips, independent overlay discovery, actual export/readback and target binding protections. Follow a concrete failure path if inspection warrants; don't inventory unrelated optional enhancements.

Scoped importer/consumer tests and full datasource tests may reuse exact-code developer results when sufficient; run targeted independent checks for the substantive claims and discovered edge cases. Record hashes, runtime origin, exact commands and bounded evidence. SS capture NOT_RUN, source-time UNKNOWN, recording stubs and undelivered WP10 native real loop are known later tasks; keep them visible as limitations without mislabelling them new defects solely due to pending work.

Return maximum3 verified findings with priority, exact path:line, trigger, proof, smallest complete correction and owner. Give consumer scope PASS/FAIL/PARTIAL, unchanged valid checks and explicit remaining real-delivery limits. Codex saves the exact final result as CONSUMERS_AUDIT_03.md. Do not claim v0.1 complete.
