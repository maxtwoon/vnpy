# Actual OpenCode consumers02C — resume partial Kimi implementation

User resumed at23:01. Codex transfers the consumer/importer/CLI/datasource paths from terminal Kimi consumers02B to this actual OpenCode worker. Kimi consumers02B exec79227 ended1 at19:51 on quota; its internal datasource subtask completed, but main handoff did not. All old OpenCode import/native assignments are terminal and superseded. Two other actual OpenCode workers own corefix03 and nativefix03 on disjoint paths. Do not edit their files or shared environment.

Read TASK_KIMI_CONSUMERS_02B.md and TASK_OPENCODE_CONSUMERS_02.md for the full original function requirements; this document supersedes actor, current owners and output handoff name. Also read KIMI_CONSUMERS_INTERRUPTION, CORE_AUDIT_02_DISPOSITION, COORDINATOR_IMPORT_REQUIREMENTS, REAL_INPUT_CANDIDATES, FUTURES_INPUT_CANDIDATES, INTERFACES, NATIVE_INTERFACES and relevant developer handoffs. Do not scan whole CLI transcripts. Preserve existing partial implementation rather than rebuild a second store.

Ownership remains importer/**,quality.py,coverage.py,cli.py,__main__.py,report.py,export.py and their tests/configs/tools; additive ../vnpy_datasource storage/CLI/tests/docs. No core/native/shared conftest/pyproject/env edits. No recorder bridge yet. No source/ledger/credentials/default .vntrader/vnpy core/site-packages/root HANDOFF/unrelated project writes, market-data network queries/accounts/gateways/git operations or provider/model/quota purchases. Do not write assistant/project memory. Personal local minimal implementation and proportionate tests.

Finish these concrete remaining paths:

1. Resume from the actual written CLI/overlay/dominant_map/StoreSink/coverage/export/report code; tests/test_cli.py was the last edited file before quota. Run scoped affected tests and correct your own unfinished work. Record failures/dependency gaps honestly; no xfail/mock replacement of actual core integration.
2. Corefix03 is adding public ConflictRecord.gap_ns. Once .coordination/core-fix03-consumer-handoff.md actually appears, preserve that public field in StoreSink/CLI serialized receipts and idempotent readback. Prove real public quarantine→freeze explicit KnownGap→reader behavior. No private sidecar parsing or guessed1ns gap. While core changes, continue independent code/tests and rerun affected integration after handoff.
3. Finish actual importer capabilities: latest overlays remain independent hashed assets/conflicts; dominant_map/trading-date join and date-unique Zhengzhou decoding; capture/table/range-bound SS time evidence plus queryable unknown-label candidates; table/month input recovery units versus product/month canonical futures partitions; store-bound quality/coverage. ALL133 deterministic repaired ETF keys excluded DEFAULT qualified data, with old/new/refetch candidates retained;118/119 disputed subsets are distinct. Do not silently broaden or narrow these requirements.
4. Use existing real Client stock/ETF/Tushare JSON locators for direct target=store publish/freeze/readback, preserve historical metadata/request/attempts/registry/fetched_at and no repeated unit conversion/network call. Test NULL independently with labelled synthetic data. Required real source-boundary checks must not be represented by CountingSink alone. The complete delivered same-snapshot Database/Alpha loop belongs to WP10 after nativefix03 is accepted; do not claim it finished from native unit tests or file presence. If you can perform a useful representative boundary check now with stable APIs, record it precisely without prematurely qualifying the whole delivered system.
5. Exercise every original CLI/export/report requirement, including actual export write/readback and empty-or-same-binding target protection. Preserve legacy datasource SQLite/Alpha behavior, non-ok refusal and pre-zero-fill target branching. Recording commands remain honestly pending/nonzero until later journal APIs exist; final task wires them, never fake success.
6. Complete scoped lint/typing/tests in delivered .venv (read-only Studio Ruff permitted); do not install into shared env. Report actual API requirements in consumer-core-requests.md without overwriting historical entries. Write .coordination/opencode-consumers02c-handoff.md with changed paths, real checks, failures, exact unfinished original requirements and WP10 boundary. Explicitly distinguish real10GB consistentSS capture/fullstreaming capability from fullhistorical import, source-time uncertainties, real consumer delivery and later recorder work. Do not relabel required unfinished consumer functions as future enhancements.

This is developer completion evidence; actual Claude review remains separate. Core/native workers must not be bypassed. No coordinator task/plan/disposition edits.
