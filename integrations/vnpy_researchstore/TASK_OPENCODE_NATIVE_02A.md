# OpenCode independent development assignment02A — native adapters

Dispatch status: OpenCode process terminated quota exit1 on2026-09-16; ownership transferred exclusively to TASK_KIMI_NATIVE_02B.md. This file remains the functional specification, not permission to restart the old writer.

You are a separate actual OpenCode worker chosen by the coordinator to parallelize a disjoint package. User roles remain: Codex plans/co-ordinates; Kimi and OpenCode develop; actual Claude Code independently audits/tests. Read IMPLEMENTATION_PLAN.md mandatory amendments, PLAN_AUDIT_DISPOSITION.md, TIME_CONTRACT_DISPOSITION.md, INTERFACES.md, BASELINE_VERIFICATION.md and root AGENTS. Personal research, useful local integration, no extra service/governance framework.

Kimi is actively implementing/fixing research_store core; another OpenCode session owns importers/quality/coverage and will own CLI/datasource/export/report. Core public reader/snapshot contracts are defined; some tests still fail and time-nullability is being corrected. Develop the actual native consumers now, but do not claim final integration PASS against a moving core or replace it with a second storage implementation.

Exclusive ownership: vnpy_researchstore/__init__.py,database.py,alpha.py,bootstrap.py (optional native utility module if needed); tests/test_native_*.py; tools/native_*.py or native_*.ps1; configs/native_*.json templates; NATIVE_INTERFACES.md. Write .coordination/opencode-native02a-handoff.md and dedicated .coordination/native-core-requests.md for exact missing core API/dependency requests. Do NOT edit research_store/**,pyproject.toml,shared conftest,importer tests,../vnpy_datasource,CLI/export/report,recorder app/UI,coordinator docs or another worker's request file. No package installs into the shared venv while Kimi owns it: request missing deps.

Use D:/repo/vnpy/integrations/vnpy_researchstore/.venv/Scripts/python.exe, repo-root cwd for development. Read current repository native contracts and installed CTA/Portfolio code rather than guessing their internals. Runtime bootstraps choose D:/repo/vnpy explicitly and record actual module path, not just4.4.0 version. Do not edit core/site-packages/default .vntrader/raw inputs/ledger/credentials/archived Chan/root HANDOFF, perform git lifecycle, or connect accounts/gateways/live strategies.

## Required behavior

- Database implements BaseDatabase read-only and binds a single immutable snapshot. Actual native1m/1h/1d only;5/15m excluded overview with diagnostics, never relabelled1m. load_tick_data explicitly UnsupportedCapability v0.1; all save/delete operations fail. Overview only uses bound snapshot. Dataset ambiguity is error, no source preference/latest fallback.
- Require usable OHLCV/units/time/price semantics. Default rejects missing applicable auxiliaries; explicit OHLCV-only mode returns NaN+extra/receipt for missing auxiliary values, never fake0. Stock OI not_applicable may be explicitly0 with metadata; missing futures OI is not that case. Keep snapshot,dataset,source,quality/time provenance in extra.
- Exactly convert native inclusive end to core half-open range or precise <= filtering. Same requirement for Database AND BOTH Alpha methods. Test actual final timestamp==end and nonaligned boundary; do not pad an entire bar/day.
- ResearchAlphaLab read-only marketdata; only1m/1d. Separate snapshot-bound lab resultdir, reject incompatible reuse. Preserve native extended_days, output columns and OHLC first-price normalization while retaining distinct original VWAP preprocessing. Empty requests/windows stable. Equity verified turnover/volume; futures verified effective multiplier and single-side volume units. Zero volume=>missing, missing/untrusted turnover/unknown multiplier=>explicit refusal. Candidate unknown trading_date/price/time semantics cannot silently become qualified backtest data.
- Bootstrap new process per snapshot. Parse/validate config, explicitly select repo path and isolated cwd BEFORE importing vnpy.trader.utility or anything transitively importing it. The package __init__ must not accidentally defeat that order. Explicitly load plugin to avoid get_database's swallowed ModuleNotFoundError/SQLite fallback; reject initialized singleton, set process-local nonsecret settings, assert actual plugin class and snapshot ID, THEN import CTA/Portfolio backtester. No changing current settings files or hot-switching snapshots. Produce run receipt with config identity/module origin/snapshot/adapter/range.
- Provide usable native entry commands and a small stable NATIVE_INTERFACES.md for other OpenCode CLI/report integration. Do not write fake success stubs for missing core behavior; expose exact request and allow independent work to continue.

## Development verification and handoff

Run scoped native tests/lint with meaningful assertions: readonly/missing field/quality refusal, inclusive/nonaligned/extended end, native interval exclusions, empty/zero volume/multiplier/MA-style untrusted turnover, bootstrap no fallback/defaultDB side effects/singleton rejection, two separate snapshot processes and bound resultdir. Use temporary test stores and offscreen imports where needed; no existing default runtime mutation. Real-data two-consumer qualification belongs to later WP10 and Claude independent review, but deliver actual working consumer code capable of it. Record existing core/environment blockers rather than marking skipped cases passed; re-run affected integration once corrected.

Existing real Client inputs and A2505 mapping locators are .coordination/REAL_INPUT_CANDIDATES.md and FUTURES_INPUT_CANDIDATES.md. Those are preparation notes, not accepted storage receipts; preserve all limitations. Final handoff must list changed paths, actual APIs, tests/results, missing dependencies/core requests and remaining integration work. Do not claim entire v0.1 complete.
