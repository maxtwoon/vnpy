# Actual Kimi journal correction02G — three Claude findings

Resume the original actual Kimi journal02E session only after it and actual Claude journal02E are terminal (both now exit0). Own the same journal.py/journal_models.py/session_recovery.py, their named journal/recovery tests and JOURNAL_INTERFACES/journal02-api-handoff docs. Another Kimi owns qualified policy in existing core/native paths; another owns NEW recorder bridge/UI files. No shared models/catalog/revisions/__init__/INTERFACES/CLI/pyproject/environment/conftest or other owner edits. Codex coordinates, Kimi develops, Claude reviews. Personal minimal practical correction, no new framework.

Read JOURNAL_AUDIT_02E.md, its disposition and all3original reviewer repros in .coordination/review-journal02e. Fix all accepted findings with meaningful regressions. Do not edit/overwrite original audit/repro artifacts; use owned tests or a separate .coordination/journal-fix02g evidence folder for adaptations.

1. F1: transition the recovered old session durably to UNCLEAN_END while still holding exclusive ownership, before closing/releasing the OS lock. No concurrent opener may admit/commit under the retiring identity in a recovery gap. Preserve correct committed-only report, linked optional successor and idempotent recovery. Prove the original forced-delay/interloper race cannot add an unreported event.
2. F2: admission's assigned sequence and successful enqueue must be atomic relative to cutoff latch/writer-stop. An accepted receipt cannot name an event stranded after the writer exits. Queue-full/error must not create phantom accepted sequence or silently leave backlog. Preserve the callback's bounded/nonblocking admission and the controlling thread's bounded close budget (including lock acquisition); do not replace a race with an indefinite wait/deadlock. Prove exact cutoff, concurrent admit/close, timeout/retry and error counters reflect reality. Use deterministic synchronization in tests, not fragile sleep-only luck.
3. F3: same(session,seq) replay equality includes event_ts_ns and source_event_id as well as instrument/kind/payload. Identical replay remains idempotent; changed event time or source identity is JournalConflictError, never silent coalescing. Preserve original timestamp precision and received/audit-time semantics honestly.

Run the focused new regressions, existing20journal tests, scoped type/lint with existing .venv/read-only Studio Ruff. Re-run/adapt the original3repro triggers without modifying the original scripts. Do not run a moving whole package or qualified/native/recorder suites; independent core helper evidence is reusable. No false pre-existing/flaky dismissal of actual repros.

No Git mutations whatsoever (especially stash/pop/reset/restore/checkout/clean), no test isolation via Git, no assistant/project memory writes, no coordinator task/status/plan/audit/disposition changes. No raw/ledger/credentials/default .vntrader/vnpy core/site-packages/root HANDOFF/archived/unrelated-project/provider/network/account/gateway/model/quota changes or installs. Terminate only your own bounded test children.

Write .coordination/kimi-journal-fix02g-handoff.md with each F1/F2/F3 disposition, exact changes/APIs/commands/results, whether public signatures changed, remaining follow-ups and LIVE_NOT_RUN. Notify the recorder owner through a named API note if any public call changes; do not edit its files. Actual Claude will perform one focused closure, then aggregation02F can proceed. A fixed foundation is not full recording or v0.1 delivery.
