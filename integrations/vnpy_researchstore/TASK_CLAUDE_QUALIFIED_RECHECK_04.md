# Actual Claude focused qualified-policy recheck04

Dispatch only after actual Kimi qualifiedfix04 terminates with .coordination/kimi-qualified-fix04-handoff.md and no writer in its changed paths. A separate Kimi journal foundation may continue only in its explicitly disjoint new modules/tests/docs; do not review/run that moving work. Actual Kimi/OpenCode develops, actual Claude reviews, Codex coordinates. Personal research, practical minimum closure of one accepted HIGH defect.

Read CONSUMERS_AUDIT_03.md, its disposition, TASK_KIMI_QUALIFIED_FIX_04.md, developer handoff and the exact original repro. Documentation-only deferral and opt-in enforcement with permissive defaults do not meet the binding approved requirement. No product/dependency/assistant-memory edits anywhere; temporary reproductions only .coordination/review-qualified04. No provider/network/account/gateway/raw/ledger/credentials/default .vntrader/core-vnpy/site-packages/root HANDOFF/git activity. Use plugin .venv and read-only Studio Ruff.

Independently verify that default freeze/read/export/native Database and BOTH Alpha paths cannot return any repaired-key row as qualified data. A deterministic repair matching upstream still counts among ALL133;118/119 disputed subsets never narrow the rule. Re-run the original3-row repro and a meaningful both-consumer/CLI case. Default callers must not have to run a quality report first. Explicit KnownGap allowance permits the missing interval only, never the excluded row; clean query windows remain usable. Candidate observations and old/new/upstream provenance survive through explicit inspection. Software/fixture evidence is not real WP10 qualification.

Check the chosen implementation against existing immutable-snapshot and publication contracts: policy/gaps bind to captured heads/files, readers do not consult mutable current heads/quality, old snapshot bytes/meaning are preserved. If an older manifest lacks policy metadata, it must be recognizably observational/unqualified or explicitly refused for default native qualified use; silently presenting legacy permissive rows as qualified would leave the defect open. New freezes must capture heads+quality consistently in one SHORT catalog read transaction; large Parquet scans, if necessary, should operate on the captured immutable file list outside that transaction rather than rereading mutable heads or holding a DB transaction for a full-history scan. No whole-history materialization workaround or hidden network query.

Run the specific affected tests/type/lint and original repro, reuse exact-code core/native recheck03 evidence for unchanged behavior. Give CLOSED/OPEN for this accepted defect, up to3 directly introduced regressions with concrete evidence if any, exact commands/hashes/limits, FINAL_VERDICT PASS/FAIL for this correction only. Codex saves your exact final result as QUALIFIED_RECHECK_04.md. Recording foundation, remaining aggregation/seal/UI and WP10 stay separate.
