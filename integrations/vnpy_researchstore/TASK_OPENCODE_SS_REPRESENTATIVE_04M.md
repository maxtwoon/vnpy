# Queued actual OpenCode WP06/WP10 SS representative completion

Dispatch only after capture04H publishes a terminal validated current-recovery receipt and scoped source inspection. Actual OpenCode develops; Codex coordinates; actual Claude audits. No new copy of the ten-GB database or whole-history count/hash merely to repeat valid unchanged evidence. Use the verified capture identity and read-only public APIs. This finishes the approved real SS import obligation; neither a schema listing nor a synthetic fixture alone completes it.

Coordinator phase release:04H is now terminal0 with final checkpoint and valid current receipt. Phase1 may prepare independent bounded real-source comparisons and NEW helper/config/evidence while04L finishes importer changes. Execute public import acceptance only after root releases04L hashes. If phase1 reaches that gate, return a truthful provisional handoff and exit; do not idle waiting. There is no additional standalone capture audit gate before04M: actual final Claude reviews the combined capture and SS evidence. Read delivery04h-final-checkpoint.json for corrections to the developer handoff's abbreviated SimNow keys and interruption wording; exact inspect JSON is authoritative.

Exclusive writes: NEW tools/delivery_ss_representative.py, its focused test if meaningful, NEW configs/delivery_ss_*.json, .coordination/delivery04m-dev and opencode-ss04m-handoff.md, D:/quant-data/reports/delivery04m and unique instance configs, immutable store outputs via public APIs. Existing product modules/helpers/source data are read-only.04L currently owns futures-related importer edits; prepare source selection independently, but execute acceptance only after its adapter/core_bridge/normalize checkpoint is released. No private helper, consumer-side normalizer or fabricated profile to work around a missing public API; report concrete defects to root for bounded ownership transfer.

Read IMPLEMENTATION_PLAN, TIME_CONTRACT_DISPOSITION, existing SS LabelEvidence/LabelProfile and importer/public StoreSink contracts, capture04H actual handoff/receipt, corrected inventory04D/04E. Preserve exact capture/table/range/provenance and source-label versus normalized-time distinctions.

Demonstrate a small real contract's selected source-label window using actual 1m versus 5m/15m OHLCV comparison. Reuse the prior investigated windows if recorded; otherwise inspect a bounded indexed sample. Only the conclusion actually proved over that capture/table/range may supply START/END bounds. Publish the evidenced supported range with original labels, all27raw columns/NULLs and real_symbol retained, trading_date UNKNOWN unless source mapping really supplies it. Outside-scope/no-evidence rows stay public inspection candidates. Include an actual unsupported/ambiguous label test; do not infer a global label convention or permanently omit SS canonical capability. Record profile identity/hash and actual input hash in config/receipt. Verify profile cannot be used on another capture/symbol/range; a missing public guard is a concrete defect, not something to patch in the runner.

Prior read-only source investigation located three continuous-series comparison windows: rb888 on2022-01-04 09:00, if888 on2024-01-02 09:30, ma888 on2022-01-04 09:00. Five1m rows matched same-label5m OHLC/volume/amount/last-OI there. These are locators only, not proof for a new capture, real contract or extended range; verify the exact bounded capture input before applying any profile. Real contract candidates include rb2605 (source span2026-02-24 13:30 to2026-05-15 14:59) and if2606 (2026-02-24 11:29 to2026-06-22 14:59); the latter first row had volume0/nonzeroamount and must not silently qualify. Do not conflate continuous evidence with executable contract prices.

Keep 1/5/15m separate in dataset specs/partitions/query outputs; 5/15m must not masquerade as native MINUTE. Real contracts, 888/777 continuous and simulated staging stay separate source/identity kinds. Demonstrate the exact8 SimNow main keys through the public classifier/quarantine path, ordinary overlaps preserved, staging independent; reuse bounded source detector evidence where identical. Unknown roll/adjustment semantics remain explicit.

Use a bounded actual MA sample to show bad amount preserved raw, canonical turnover NULL/untrusted, OI chosen from the evidenced field rather than assuming openint is total, and default VWAP-dependent consumption refused. Do not turn missing calendar/listing/session/roll/adjustment evidence into a READY label; report observation/candidate versus backtest eligibility separately. No live gateway connection.

For the supported real contract canonical range, exercise public publish/repeat-import/freeze/query, actual revision/snapshot IDs and count reconciliation; prove original snapshot immutable if extension creates a new revision. Retain raw instrument case and resolved identity evidence without decade guessing. Coverage includes observed/accepted/quarantined/candidate and expected UNKNOWN where unsupported. Record actual timings/peak memory if measurable, no invented performance target.

Handoff with exact commands/exits, code/dependency hashes, input/capture/table/range/profile IDs, output IDs/paths, comparisons and limitations. Focused runner tests/lint only; final broad integration checks belong WP10 delivery after all source is stable. No Git/memory/raw/ledger/credentials/defaultsettings/vnpycore/sitepackages/rootHANDOFF/archived/unrelated-project changes; no network/provider/account/gateway/purchases or shared environment install.
