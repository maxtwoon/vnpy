# Actual OpenCode recording core02C — queued, not dispatched

Codex may dispatch only after actual OpenCode corefix03 has finished, its public event-identity handoff is available, and actual Claude core recheck03 closes the blocking findings. This document does not grant an active writer until dispatch. Kimi's current quota limitation does not shrink the approved recording scope; actual OpenCode takes this bounded development assignment when dispatched. Codex coordinates and actual Claude audits. Personal local research, practical implementation and proportionate tests.

Read TASK_KIMI_RECORDING_02.md for the full functional requirements; this wrapper supersedes actor, handoff filename and the old illustrative signatures only. Read IMPLEMENTATION_PLAN mandatory amendments, INTERFACES, core-fix03-consumer-handoff.md, opencode-core-fix03-handoff.md and CORE_RECHECK_03.md. Implement all journal, aggregation, session recovery, replay, sealing and retention requirements. Do not substitute typed stubs or examples for working APIs.

Own research_store/journal.py, aggregation.py, session_recovery.py, sealing.py and their tests. With the prior core writer terminal and reviewed, own the minimum necessary core models/public exports/catalog/INTERFACES additions for this recording milestone. Preserve the reviewed conflict-gap, recovery and session/sequence tick identity behaviors; report affected changes for later review. The bridge, UI, CLI, importers, native consumers, datasource and their tests remain separately owned. Do not modify shared environment or packaging here; raise an exact need to Codex. No coordinator plan/task/status files.

Publish .coordination/recording02-api-handoff.md early, with exact import locations and typed signatures for session creation, immutable copied event admission and assigned sequence, commit/status/watermarks/errors, close_at_cutoff/timeout/retry, recover_session, replay, seal and retention eligibility. Preserve the existing public store-based call pattern recover_session(store, session_id) and seal(store, request) unless a necessary explicit compatibility amendment is documented. Define the request sequence range, transform version, source identity and calendar/spec bindings. Distinguish frozen interface from implemented/tested behavior so the recorder bridge can safely code in parallel only after Codex assigns it.

Journal event identity and canonical sealed ticks must use the reviewed stable session/ingest-sequence contract. Do not derive event IDs from timestamps or current read order. The bridge calls public journal APIs only; both owners exchange exact gaps through handoff notes instead of editing each other's internals. The accepted stop barrier remains the bridge owner's responsibility; journal close must support its exact committed cutoff and retry semantics.

No assistant/project memory writes, provider/model/account changes, purchases, market-data network queries, gateways/accounts, raw-source/ledger/credentials/default .vntrader/vnpy core/site-packages/root HANDOFF/unrelated-project/git writes. Use package .venv and read-only Studio Ruff; no dependency installs. Write .coordination/opencode-recording02c-handoff.md with actual API and command results, failure-mode evidence, changed files, remaining issues and explicit live-gateway NOT_RUN. A developer handoff is not independent audit acceptance or whole-v0.1 completion.
