# Coordinator requirement clarification — timezone is not a trading calendar

Observed during current02I API handoff preparation, not an independent Claude approval or a request for a new calendar service. Original IMPLEMENTATION_PLAN/TIME_CONTRACT_DISPOSITION and02I task prohibit natural-date inference of trading_date; that includes a localized natural date, not merely a UTC date.

The current revised handoff says timezone-only calendar_spec labels local dates. Current aggregation._trading_date_from_calendar returns datetime(..., ZoneInfo(spec)).date() into trading_date; sealing also calls it for ticks. A Friday-night futures event thereby acquires Friday as trading_date merely from Asia/Shanghai. A timezone cannot establish next trading day, holidays, or session applicability. The comments saying no natural-date inference do not make this valid evidence.

Before02I final acceptance: timezone-only specifications may establish display timezone/normalized UTC conversion, but trading_date must remain NULL with explicit uncertainty unless real source trading_date or explicit scoped calendar/session mapping supplies it. Preserve a genuinely provided source date/mapping and its provenance; do not globally infer day+1 or fabricate a full market calendar. If an explicit minimal mapping form is supported, document its version, scope and validation; synthetic fixture evidence must be labelled synthetic. Verify Friday-night/holiday-sensitive unknown case and an explicitly sourced mapped day, for both tick and bar outputs. Missing calendar must not be described as proven out-of-session (unknown versus false are distinct).

This is within existing02I F4 source/calendar semantic obligation. Actual developer must correct the owned implementation/tests/docs; no Codex product edits. Actual Claude02J will independently verify this requirement along with F1-F4.03D may integrate asset/unit/recover interfaces now but must not adopt timezone-only trading_date claims or finalize affected E2E until corrected source handoff. Preserve all original02H/02I evidence and partial code; no Git rollback or new service.
