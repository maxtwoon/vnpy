# Claude Code bounded contract clarification — unknown source times

Actual Claude Code reviewer; no product edits or tests needed for this bounded design clarification. Kimi core and OpenCode importer implementation are actively in progress, so this is NOT an implementation acceptance review. Read IMPLEMENTATION_PLAN.md, mandatory amendments, INTERFACES.md, research_store/schemas.py, relevant importer normalization/core_bridge (if present), and .coordination/opencode-core-requests.md if present. Do not read CLI jsonl transcripts, credentials or bulk raw data. You may read small relevant source metadata/README if needed; no source scripts/network.

Observed integration question: OpenCode reports BARS_SCHEMA_V1 requires non-null bar_start/bar_end/trading_date, while some SSQuant/RQ contract source ranges do not establish trading_date or source START/END label. The approved plan requires unknown metadata remain honest, real imports to work when sufficient selected-scope evidence exists, and candidate observation query access without masquerading as qualified backtest input. It also requires daily key=trading_date and native consumers to refuse ambiguous units/time semantics. The already-verified ETF END semantics are not in dispute.

Check the actual contracts and distinguish source facts: source event/local label, normalized interval start/end, trading day, quality/qualification. Propose the smallest coherent clarification and any exact schema/API change needed, including where to reject or preserve unnormalizable observations, whether a nullable trading_date is needed for minute candidates, and how query/freeze/consumer policy should handle it. Do not infer a calendar or label from date alone. Do not redesign into a service or sidestep promised candidate availability by simply deleting source rows.

Return: whether there is a genuine contract gap; minimal resolution with affected owner/files/API and invariants/tests to add; which existing source paths can proceed now vs need explicit metadata. At most3 concrete points. Codex will issue instructions to the actual developers after your result. Do not describe incomplete ongoing code as a final failed implementation or approve all storage functionality.
