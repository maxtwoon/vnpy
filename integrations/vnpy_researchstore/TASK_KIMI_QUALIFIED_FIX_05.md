# Actual Kimi qualified correction05 — close three actual Claude findings

Continue original qualifiedfix04 developer session; actual Claude QUALIFIED_RECHECK_04.md FAIL is now final. Read exact report and QUALIFIED_RECHECK_04_DISPOSITION. Fix ONLY its3 accepted regressions, preserving all reviewed working default-exclusion behavior. Codex coordinates, Claude audits. Personal local use, focused implementation and tests.

Exclusive writes: minimum research_store/snapshots.py, reader.py, quality.py, vnpy_researchstore/native_common.py and necessary database.py/alpha.py handling, related default-qualified/native/snapshot tests, INTERFACES.md/NATIVE_INTERFACES.md and your handoff. Do not edit models/catalog/revisions/store/__init__/journal/aggregation/seal/recorder/UI/CLI/importers/datasource/pyproject/environment/shared conftest or other owner files. If a necessary change exceeds this scope, state exact request before touching it. Journal02G is under actual review; recorder03C and delivery04A are live on separate files. No whole-moving-package suites.

F1 HIGH: resolve_bar_dataset exchange validation currently calls default reader.bars with no start/end and rejects clean query windows when another interval of the SAME symbol is excluded. Actual repro .coordination/review-qualified04/repro_wider_scope_clean_range.py must pass for core, Database and BOTH Alpha methods with separate clean/excluded days. Preserve complete per-batch exchange validation, including empty-leading batches and mixed/missing exchange labels (native03 independently fixed these). A scoped discovery/observational validation implementation is acceptable only if actual consumer data reads still enforce default qualification for their effective requested range, including Alpha extended_days. Never globally allow gaps or return excluded rows. Test clean separated range, selected excluded range refusal, explicit KnownGap leaves rows absent, all3public native calls and both exact/nonaligned endpoints.

F2 MEDIUM: capture immutable heads/full file manifests and catalog quality facts in ONE SHORT catalog transaction; exit transaction before file hashing/Parquet row scans. Then compute exclusions from those captured immutable references and publish manifest without rereading mutable current heads/quality. Keep snapshot determinism and all-selected-dataset consistency. Add a meaningful synchronization test proving scan is outside transaction/lock and later head changes cannot mix into frozen capture. Avoid a whole-history in-memory workaround.

F3 MEDIUM: old manifest lacking policy capture must be distinguishable from captured-clean. Preserve old bytes and previous explicit observational access; provide an explicit typed/public qualification status or a specific default refusal. Default qualified/native reads must not silently present legacy permissive repaired rows as qualified. New clean snapshots remain usable. Test old-snapshot default/native rejection (or unmistakably enforced unqualified contract), explicit observational read preservation and old bytes unchanged. Do not merely leave an empty-list accessor plus docs or retroactively mutate snapshots.

Use stable original reviewer repros unchanged; place new regression evidence in your own .coordination/qualified05-dev folder, not misleading reviewer folders. Tests/type/lint scoped to affected implementation and prior native exchange/time behavior. No Git mutations of ANY kind: no stash/pop/drop/reset/restore/checkout/switch/clean/add/commit/worktree; integrations are untracked and concurrent writers exist, Git stash is not isolation. No moving/renaming another suite, no fake clean baseline. No assistant/project memory writes, raw/ledger/credentials/default .vntrader/vnpy core/site-packages/root HANDOFF/provider/network/account/gateway/model/quota changes or installs.

Finish .coordination/kimi-qualified-fix05-handoff.md with files, eachfindingclosure/evidence, exact commands/counts/errors and known limits. Exit after bounded correction. Actual Claude will recheck; developer tests are not audit acceptance or WP10 completion.
