# Claude Code verification assignment — existing environment baseline

You are actual Claude Code, the user's independent reviewer/tester. While Kimi and OpenCode develop the new integration, establish the existing datasource and root sync baseline. This is a bounded read-only/testing assignment, not a review of their incomplete product code. Codex coordinates; do not develop fixes.

Run from D:/repo/vnpy. Read root AGENTS.md and integrations/vnpy_datasource README.md/VERIFICATION.md for the real current test entry. Do not read credential files or run source queries, gateway connections or downloaded data scripts. Do not scan .coordination/*.jsonl transcripts. Do not edit product files, root HANDOFF, ledger, existing .vntrader, raw data or developer task files. Pytest caches and temporary test artifacts are allowed; no git lifecycle.

Use D:/veighna_studio/python.exe for the existing baseline; the new package .venv may still be provisioning. Record sys.executable and module origins for vnpy and vnpy_datasource (inspect import origins without starting engines). Run the full existing integrations/vnpy_datasource/tests suite, Ruff scoped to that integration, and python tools/sync_check.py once. Existing datasource has a 24-file baseline hash record at integrations/vnpy_researchstore/.coordination/datasource-baseline.json; verify these still match before calling this a pre-change baseline. If another developer has started those edits, explicitly label the result concurrent/current rather than pristine. Importer-only work does not imply datasource edits.

Return a concise factual report with exact commands, interpreter/origins, exit statuses and counts, changed-baseline paths (if any), concrete existing failures and limitations. Distinguish pre-existing root sync failures from this new integration. Do not infer whole-project or storage acceptance from these checks. No more than the required single run unless command invocation needs correction. Coordinator will save your final report.
