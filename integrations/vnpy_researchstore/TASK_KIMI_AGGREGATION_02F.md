# Actual Kimi recording continuation02F — aggregation, sealing and public core wiring

QUEUED, not dispatched. Codex assigns a single actual Kimi developer only after qualifiedfix04 and journal02E writers terminate with actual final handoffs. The early journal API document alone is not completion evidence: it was written before journal implementation/tests finished. Use the final implemented signatures and current qualified policy; independently reviewed changes keep their existing dispositions. Actual Claude audits; Codex coordinates. Personal practical local research, no new service/framework.

Read IMPLEMENTATION_PLAN mandatory amendments, TASK_KIMI_RECORDING_02.md, JOURNAL_INTERFACES.md, INTERFACES.md, final qualified/journal handoffs and their current audit dispositions. This completes mandatory WP09 and core public integration; WP08 recorder bridge/UI/CLI remains its separate required task. Do not recast missing functions as optional future enhancements.

Own research_store/aggregation.py, sealing.py, optional retention.py, related tests, and the minimum required shared models/revisions/__init__/catalog/interface wiring after prior owners release them. Necessary journal corrections are allowed only after journal02E release and must be reported precisely. Do not edit native bridge/UI/CLI/importer/quality/datasource files, shared conftest/environment/pyproject or coordinator plans/tasks/status/audits. Pass exact integration needs through .coordination/recording02-api-handoff.md and recording-core-requests.md; no overlapping edits.

First settle exact public signatures/import locations: recover_session(store, session_id, ...) and seal(store, request), deterministic replay, range endpoint/empty-range rules, source_spec/calendar_spec string format/version/validation. SessionRecoveryReport must unambiguously identify old session, optional successor ID and successor lock ownership/closure responsibility; no silent leaked writer/lock. The bridge needs last-error details and cutoff-close timeout/retry semantics through public APIs, not only an error count or private attributes. Session-local replay sequence is not a cross-session unique key; document single-session replay unless cross-session support already actually exists, without adding it just for prose consistency.

Aggregate only committed journal events, stably ordered by event time then ingest sequence. Preserve original payload and session/sequence lineage. Required statuses: unknown first cumulative baseline, resets, reconnect/gap, late, partial final bar and out-of-session; missing event time/calendar never becomes received-time/natural-date inference. No negative-delta clamping to green. Real quote/CTP observations and sampled polling are not certified trade-by-trade market data; derived bars must retain observed-source semantics. Separate provisional running output from final session/day output. Unknown calendar permits raw tick preservation but not invented qualified trading-day bars.

Implement seal through the existing immutable canonical publish/revision API, including reviewed tick event identity and qualified policy. Request cannot include uncommitted events. Idempotency must cover session, committed sequence range, transform and all semantic/calendar/source configurations that affect output. Repeat replay/seal produces the same content/count and published identity; changed transform/input produces an explicit new revision. Late corrections use explicit revision/conflict mechanisms, never overwrite the old snapshot or silently last-win. A partial publication/failure cannot be recorded as completely sealed; preserve actionable recovery state and actual output revision/file/count evidence.

Retention: only module-owned journal files with VERIFIED complete sealing/publication, at least14days after that condition, and no active owner are eligible. A partially sealed range must not permit deletion of remaining events. Resolve and validate absolute journal paths within the intended root before any cleanup; never include raw/canonical/snapshot files or unverified symlink/junction escapes. Use temporary files/sessions for tests, do not delete real historical journals just to prove behavior. No automatic deletion just because a session is CLOSED.

Replace the existing public recover_session/seal PENDING stubs with real delegated implementations and truthful typed receipts; keep one implementation per capability. Update core exports and INTERFACES accordingly. CLI recover-session/replay/seal wiring is owned by the recorder bridge assignment; provide exact serializable requests/results/errors and sample config without editing its CLI.

Tests proportionate to new behavior: committed-only aggregation; deterministic repeat replay/seal; baseline/reset/gap/late/partial/zero volume; error/missing calendar/time; interrupted seal/recovery, late new revision and old snapshot stability; full-range retention eligibility, active/partial/unverified refusal and strict paths. Reuse unchanged journal failure-mode evidence, run affected regressions/type/lint in package .venv and read-only Studio Ruff. No installs. Deliver one actual temp-store seal→freeze→query example labelled synthetic/offline with exact IDs/counts; no fake real-data qualification.

No assistant/project memory writes, provider/model/account/quota purchases, market-data/network/gateway/account queries, raw/ledger/credentials/default .vntrader/vnpy core/site-packages/root HANDOFF/unrelated-project/git changes. Write .coordination/kimi-aggregation02f-handoff.md and final recording02-api-handoff.md with exact files/APIs/commands/evidence, unresolved requests, readiness for bridge integration and LIVE_NOT_RUN. Do not claim full v0.1 before WP10 and actual Claude final audit.
