# Actual Kimi native correction03 — two Claude native02 findings

Dispatch after developer quota is available, the actual Claude native02 process is confirmed terminal, and Codex confirms one native owner. Original Kimi native session session_5d92a90e-5689-455a-9e67-47491a5496ad is terminal/idle. Read NATIVE_AUDIT_02.md plus the detailed actual reviewer artifact .coordination/review-native02/CLAUDE_NATIVE02B_AUDIT_REPORT.md, NATIVE_AUDIT_02_DISPOSITION.md, NATIVE_INTERFACES, plan amendments and prior native handoff.

Codex coordinates; actual Kimi/OpenCode develops; actual Claude audits. Personal local minimal fixes. No source provider/network/account/gateway calls, credentials, raw/ledger/default .vntrader/vnpy core/site-packages/root HANDOFF writes, installs or git lifecycle. Own only native_common.py, database.py, affected native modules/tests/docs; no core/importer/CLI/datasource/shared fixture edits. Core fix03 may run independently after explicit coordination; use public APIs and report any actual dependency gap.

F1: exchange validation must cover the whole returned stream, not just its first RecordBatch. A later row with an unmapped or mismatched label must never be stamped silently as the requested exchange. Keep resolution/ambiguity/refusal policy explicit and consistent in both Database and Alpha. Handle an empty first batch without incorrectly concluding the stream has no rows. Use streamed validation, not whole-history materialization, and scope checks to the requested identity/range where applicable. Add focused tests with multiple batches/partitions and later unknown label, later different valid exchange, normal uniform labels, and empty first batch; exercise both consumers that share the helper. Reuse .coordination/review-native02/repro_mixed_exchange.py as reviewer evidence.

F2: DAILY overview start/end and daily identity/count semantics must use trading_date in the same native timezone/date-label convention as load_bar_data. Do not derive daily identity from normalized bar_start. MINUTE/HOUR overview still uses real bar-start semantics. Add a night-session daily example whose UTC/local bar_start date differs from trading_date, verify overview boundaries agree with actual native reads and shipped native_overview output, and ensure minute/hour behavior remains unchanged. Reuse repro_daily_overview_mismatch.py.

Run the39 existing native tests plus new focused regressions, scoped mypy and Ruff (read-only Studio Ruff allowed); no whole moving integration suite. Preserve pure lazy import, isolated bootstrap and no fallback, exact inclusive boundaries for Database/BOTH Alpha methods, NULL/unknown/unit checks and snapshot-bound output. Do not expand to native tick backtesting or new global discovery indexes.

Write .coordination/kimi-native-fix03-handoff.md with findings closed/open, changed files and actual commands/results; update NATIVE_INTERFACES only where behavior/API needs clarification. Actual Claude will re-run the two reproductions and affected checks on stable code; developer green results alone do not close independent findings. Real WP10 two-consumer qualification remains separate.
