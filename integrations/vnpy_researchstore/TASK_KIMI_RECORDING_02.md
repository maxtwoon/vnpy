# Kimi development assignment 02 — durable journals, aggregation and sealing

Dispatch after core01 handoff and addressing any blocking Claude core findings. Read plan/audit amendments/INTERFACES and preserve OpenCode ownership. Own research_store/journal.py,aggregation.py,session_recovery.py,sealing.py and their tests; update own core interfaces minimally where necessary with explicit handoff. Do not edit recorder UI/VeighNa bridge or CLI owned by OpenCode. Actual Kimi implementation, not Codex substitution.

Implement real APIs recover_session(root,session_id)->SessionRecoveryReport; seal(SealRequest)->SealReceipt; deterministic replay of committed sequence. Expose typed journal session operations required by external recorder: append/commit watermark/close_at_cutoff/status/recover, errors and OS-exclusive ownership. Write API additions to INTERFACES early.

One session SQLite WAL/FULL, single writer, cross-process filelock released on process death, no stale-PID stealing. Events key session+seq and immutable payload hash; identical replay no duplicates, changed payload samekey error. Event insert and committed_seq same transaction. Bounded queue100k,250ms/1000batch target, visible accepted/committed/rejected/errors; no indefinite event-thread blocking or false clean shutdown. Closing confirms last accepted cutoff committed; failed close not CLOSED. Crash old UNCLEAN_END/new linked session.

Aggregate only committed events, stable eventtime+seq ordering. Session/day final output; provisional bars excluded. Full original payload retained. cumulative volume/amount reset, initial baseline, reconnect, source type, calendar/trading day, late/partial/outofsession statuses explicit. TickData/CTP snapshots not guaranteed every trade; classify observed aggregate; polling quote snapshots cannot be promoted to full market OHLCV. No synthetic filling/negativeclamp green. Unknown calendar prevents qualified tradingday bar, not raw tick capture.

seal input session+watermark range+transform/calendar/spec idempotency; new canonical revision via tested publication APIs. Never alter old snapshot on late correction. Replay twice samecontent/count. Retention only14days after verified sealing+publication; only module-owned journal files, root boundary checks, never raw/canonical. No automatic journal deletion merely because sessionclosed.

Meaningful tests include competing process lock, same timestampmulti events, same seqcollision, queuefull/error, commit interrupted, childprocesskill recovery, exact cutoff, replay twice, reset/gap/firstbaseline/zero/late/partial, repeatedseal/late newrevision, retention eligibility. Record actual test/lint results. Write .coordination/kimi-recording02-handoff.md with APIs for OpenCode's recorder bridge.
