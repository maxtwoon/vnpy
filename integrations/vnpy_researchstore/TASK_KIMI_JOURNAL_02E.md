# Actual Kimi recording foundation02E — disjoint journal ownership

Codex dispatches this NEW actual Kimi session after core/native correction03 independently PASS. Another actual Kimi session owns repaired-data default enforcement under TASK_KIMI_QUALIFIED_FIX_04.md, including existing core/native/quality/CLI paths. You may run concurrently only in the new journal scope below. Personal local research: minimal practical durable journal, no new service/framework. Codex coordinates, actual Kimi develops, actual Claude audits.

Read IMPLEMENTATION_PLAN mandatory amendments, FIX03_RECHECK_DISPOSITION.md, TASK_KIMI_RECORDING_02.md and current INTERFACES. This is the foundation stage of the full approved WP08/WP09 task; aggregation/sealing/retention and bridge/UI remain mandatory follow-ups, not optional enhancements or cancelled scope.

Exclusive writes: NEW research_store/journal.py, optional NEW research_store/journal_models.py, NEW research_store/session_recovery.py, tests/test_journal*.py, tests/test_session_recovery*.py, JOURNAL_INTERFACES.md and .coordination/journal02-api-handoff.md / kimi-journal02e-handoff.md. Do not edit existing models.py, schemas.py, catalog.py, revisions.py, snapshots.py, reader.py, store.py, __init__.py, INTERFACES.md, any importer/quality/CLI/export/datasource/native path, shared conftest/pyproject/environment, or their tests. Those belong to the qualified-fix owner or later explicit integration. Reuse existing core interfaces; if a necessary shared change is missing, document exact proposed API in .coordination/journal-core-requests.md and continue independent work. No overlapping edits. Do not overwrite an existing file on this NEW list without first reporting the ownership collision.

Implement the actual journal/session/replay/recovery foundation from TASK_KIMI_RECORDING_02.md: per-session SQLite WAL/FULL, exclusive cross-process OS lock (released on process death, no stale-PID stealing), copied immutable payload keyed by session+assigned ingest sequence, payload hash and collision refusal including a different instrument under the same session/seq. Event insertion and committed watermark in ONE transaction. Bounded100000queue, 250ms/1000batch target, visible accepted/committed/backlog/rejected/errors; overflow/write error stops admission, no indefinite event-thread blocking. Explicit close_at_cutoff supports exact accepted cutoff, bounded10sec close failure and retry, no false CLOSED. No callback mutation may change the saved payload.

Expose exact typed operations for create/open/session identity/admission/assigned seq/status/commit/error/close_at_cutoff/retry and recover_session; use store-based public patterns where compatible with the existing contract. Recovery reads committed events only, marks the unclosed old session UNCLEAN_END, creates a separate linked session when requested, and never treats uncommitted rows as accepted durable events. Deterministic replay is event-time then sequence, with stable cross-session tie handling if applicable; canonical snapshot ordering remains a different reviewed contract. Preserve full original payload, source/gateway/event kind, real identity and original time precision; no gateway connection or synthetic production claims.

Publish JOURNAL_INTERFACES.md and .coordination/journal02-api-handoff.md early, distinguishing frozen signatures from implemented/tested behavior. Keep typed helper models in your owned new module if necessary until the explicit shared-core integration phase; do not replace the existing public stubs or claim CLI recover-session/seal now work. The next aggregation/sealing assignment will integrate those required public paths after the qualified-fix writer releases them.

Run meaningful actual tests in package .venv: competing child-process lock, same-sequence immutable payload/collision, same-time distinct events, bounded queue overflow/writer error, interrupted commit, bounded childprocess kill and committed-only recovery, exact cutoff/timeout/retry, replay twice and input mutation. Use only task-owned child processes and temporary stores; no termination of unrelated Python/trading processes. Type/lint owned files with read-only Studio Ruff, no installs. No need to run archived or unrelated moving suites.

No assistant/project memory writes, coordinator TASKS/plan/audit/disposition/task edits, source/ledger/credentials/default .vntrader/vnpy core/site-packages/root HANDOFF/unrelated-project/git writes, market-data/provider/network/account/gateway connections, provider/model changes or quota purchases. A provider refusal stops this attempt honestly; no self-retry loop/fallback. Write .coordination/kimi-journal02e-handoff.md with exact APIs/files/tests/commands/results and explicit remaining aggregation/seal/retention/public wiring/bridge obligations. Live gateway remains LIVE_NOT_RUN.
