# Actual Kimi core correction03 — Claude F1/F2/F3

Dispatch only after developer quota is available and Codex confirms one core owner. The original Kimi core session session_1ba21b09-1fdf-443e-96a2-556da7a92903 is terminal/idle, not running. Actual Claude core02 also terminated. A separate native review may still be running read-only; coordinate any shared-core changes with Codex so affected review evidence is not mistaken for a stable version. Codex plans, actual Kimi/OpenCode develops, actual Claude audits. Do not start recording02 in this task.

Read CORE_AUDIT_02.md, CORE_AUDIT_02_DISPOSITION.md, IMPLEMENTATION_PLAN mandatory amendments, INTERFACES and prior core handoffs. Personal local minimum fixes; no new services, provider/network/account connections, raw data/ledger/default .vntrader/vnpy core/site-packages/root HANDOFF changes or git lifecycle.

Own core schemas/models/catalog/objects/revisions/snapshots/reader/store/public exports and corresponding core tests/INTERFACES. Do not modify importers/quality/coverage/CLI/export/report/datasource/native files or their tests. Use .coordination/core-fix03-consumer-handoff.md to communicate exact public API changes and required caller edits. No cross-owner compatibility bypasses.

1. F1: add typed public conflict gap bounds using the same actual normalized bar bounds already stored in evidence. Populate every applicable public receipt/serialization/recovery path. Preserve meaningful behavior for daily conflicts and ticks. Prove a caller can import conflicting bars, inspect only public receipt fields, resolve QUARANTINE, construct the explicit KnownGap allowance, freeze/query successfully while retaining the gap; without permission the requested intersecting query must fail. No parsing the private sidecar or reconstructing an assumed1ns width. Consumer owns its overlay test update after your handoff; do not edit it.

2. F2: include partially published FAILED batches in general recover visibility. Use .coordination/review-core02/repro_multipartition.py as actual reviewer evidence. Two partitions with interruption after the first commit must not falsely report whole-batch published. General sweep must identify this batch and safely resume if durable staged/manifest anchors allow; otherwise return a loud per-batch status/error and precise retry guidance. Repeating recovery and the same idempotency-key retry must converge without duplicated rows or clobbering a newer head. Corrupt/missing files remain failures. Do not auto-delete orphan or canonical files. Test both recoverable and unavailable-anchor cases as appropriate to the actual implementation.

3. F3: implement stable canonical tick event identity (session+ingest sequence) across schema, validation, dedup/conflict, partition publication, snapshot/reader output and deterministic ordering. Do not derive sequence from timestamp or unstable current query order. Two distinct events at the same timestamp must both survive even if all market fields match. Identical replay of the same event identity is idempotent; reuse of that identity with changed payload is explicit conflict/integrity failure, not a new accepted event. Preserve original timestamp precision and raw payload/provenance. Define validation/schema compatibility honestly for this unreleased version; do not invent historical session IDs silently. Native tick backtesting remains unsupported and is not in scope.

Run focused new regressions and affected existing core suites, scoped Ruff and mypy. Use read-only Studio Ruff if necessary. Public core import must remain independent of vnpy/provider modules. Preserve existing daily instrument-OR-series/trading-date identity, nullable minute day+quality and snapshot immutability. Avoid repeated full moving consumer suites; report their required follow-ups by exact API.

Write .coordination/kimi-core-fix03-handoff.md with each finding disposition, concrete API changes, exact commands/results and any unresolved core issue. Update INTERFACES truthfully. Actual Claude will recheck the three findings and changed behavior; a green developer suite alone does not close independent findings. Recording02 starts only after blockers are resolved.
