# Actual Claude Code focused native recheck03

Dispatch only after TASK_OPENCODE_NATIVE_FIX_03.md finishes with .coordination/opencode-native-fix03-handoff.md (the functional TASK_KIMI_NATIVE_FIX_03.md requirements remain binding) and no native writer. Read NATIVE_AUDIT_02.md, the detailed report/repros under .coordination/review-native02, disposition, developer handoff and changed files. Reuse exact-code valid prior findings/tests; do not re-audit the whole project.

No product edits. Use package .venv and scoped native tests; temporary repros only under .coordination/review-native03. No credentials/gateways/network provider/raw/default settings/ledger/vnpy core/site-packages/git writes. Actual Kimi/OpenCode fixes, Codex coordinates. Record reviewed file hashes, core dependency changes, commands and results.

1. Re-run mixed-exchange repro and verify a later streamed unknown/mismatched label can neither be silently relabelled nor silently evade ambiguity/refusal. Exercise Database and Alpha shared path, multiple batches, empty leading batch, normal matching data, and requested scope. No whole-history materialization workaround.
2. Re-run night-session daily overview repro. Overview dates/counts and returned daily bars must use trading_date consistently, including the shipped native_overview CLI. Minute/hour timestamps remain correct. Verify precise inclusive-end behavior remains intact for both Alpha methods and Database.

Run affected tests/lint/typing with prior valid evidence reused where appropriate. Return each finding CLOSED/OPEN with direct evidence and bounded native PASS/FAIL. Real-data WP10 NOT_RUN and known final packaging work remain separate; no whole-v0.1 or live-recording readiness claim.

Do not create, update or append ANY assistant/project memory (including .claude/projects/**/memory, .codex/memories or MEMORY.md). The user has not requested memory updates. Save task evidence only in the explicitly named review artifact directory; an auto-memory habit does not authorize extra writes.

